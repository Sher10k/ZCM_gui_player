// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by zcm-gen

#include <stdint.h>
#include <stdlib.h>
#include <zcm/zcm_coretypes.h>
#include <zcm/zcm.h>

#ifndef _ZcmSauglMssduTimestamp_h
#define _ZcmSauglMssduTimestamp_h

#ifdef __cplusplus
extern "C" {
#endif

#include "ZcmService.h"
/**
 * @struct ZcmSauglMssduTimestamp
 * @brief Временная метка от МССДУ
 * @var ZcmSauglMssduTimestamp::timestamp
 * @brief - Временная метка, количество секунд, прошедших с 1 января 1970 года
 *
 */
typedef struct _ZcmSauglMssduTimestamp ZcmSauglMssduTimestamp;
struct _ZcmSauglMssduTimestamp
{
    ZcmService service;
    int64_t    timestamp;
};

/**
 * Create a deep copy of a ZcmSauglMssduTimestamp.
 * When no longer needed, destroy it with ZcmSauglMssduTimestamp_destroy()
 */
ZcmSauglMssduTimestamp* ZcmSauglMssduTimestamp_copy(const ZcmSauglMssduTimestamp* to_copy);

/**
 * Destroy an instance of ZcmSauglMssduTimestamp created by ZcmSauglMssduTimestamp_copy()
 */
void ZcmSauglMssduTimestamp_destroy(ZcmSauglMssduTimestamp* to_destroy);

/**
 * Identifies a single subscription.  This is an opaque data type.
 */
typedef struct _ZcmSauglMssduTimestamp_subscription_t ZcmSauglMssduTimestamp_subscription_t;

/**
 * Prototype for a callback function invoked when a message of type
 * ZcmSauglMssduTimestamp is received.
 */
typedef void(*ZcmSauglMssduTimestamp_handler_t)(const zcm_recv_buf_t* rbuf,
             const char* channel, const ZcmSauglMssduTimestamp* msg, void* userdata);

/**
 * Publish a message of type ZcmSauglMssduTimestamp using ZCM.
 *
 * @param zcm The ZCM instance to publish with.
 * @param channel The channel to publish on.
 * @param msg The message to publish.
 * @return 0 on success, <0 on error.  Success means ZCM has transferred
 * responsibility of the message data to the OS.
 */
int ZcmSauglMssduTimestamp_publish(zcm_t* zcm, const char* channel, const ZcmSauglMssduTimestamp* msg);

/**
 * Subscribe to messages of type ZcmSauglMssduTimestamp using ZCM.
 *
 * @param zcm The ZCM instance to subscribe with.
 * @param channel The channel to subscribe to.
 * @param handler The callback function invoked by ZCM when a message is received.
 *                This function is invoked by ZCM during calls to zcm_handle() and
 *                zcm_handle_timeout().
 * @param userdata An opaque pointer passed to @p handler when it is invoked.
 * @return pointer to subscription type, NULL if failure. Must clean up
 *         dynamic memory by passing the pointer to ZcmSauglMssduTimestamp_unsubscribe.
 */
ZcmSauglMssduTimestamp_subscription_t* ZcmSauglMssduTimestamp_subscribe(zcm_t* zcm, const char* channel, ZcmSauglMssduTimestamp_handler_t handler, void* userdata);

/**
 * Removes and destroys a subscription created by ZcmSauglMssduTimestamp_subscribe()
 */
int ZcmSauglMssduTimestamp_unsubscribe(zcm_t* zcm, ZcmSauglMssduTimestamp_subscription_t* hid);
/**
 * Encode a message of type ZcmSauglMssduTimestamp into binary form.
 *
 * @param buf The output buffer.
 * @param offset Encoding starts at this byte offset into @p buf.
 * @param maxlen Maximum number of bytes to write.  This should generally
 *               be equal to ZcmSauglMssduTimestamp_encoded_size().
 * @param msg The message to encode.
 * @return The number of bytes encoded, or <0 if an error occured.
 */
int ZcmSauglMssduTimestamp_encode(void* buf, uint32_t offset, uint32_t maxlen, const ZcmSauglMssduTimestamp* p);

/**
 * Decode a message of type ZcmSauglMssduTimestamp from binary form.
 * When decoding messages containing strings or variable-length arrays, this
 * function may allocate memory.  When finished with the decoded message,
 * release allocated resources with ZcmSauglMssduTimestamp_decode_cleanup().
 *
 * @param buf The buffer containing the encoded message
 * @param offset The byte offset into @p buf where the encoded message starts.
 * @param maxlen The maximum number of bytes to read while decoding.
 * @param msg Output parameter where the decoded message is stored
 * @return The number of bytes decoded, or <0 if an error occured.
 */
int ZcmSauglMssduTimestamp_decode(const void* buf, uint32_t offset, uint32_t maxlen, ZcmSauglMssduTimestamp* msg);

/**
 * Release resources allocated by ZcmSauglMssduTimestamp_decode()
 * @return 0
 */
int ZcmSauglMssduTimestamp_decode_cleanup(ZcmSauglMssduTimestamp* p);

/**
 * Check how many bytes are required to encode a message of type ZcmSauglMssduTimestamp
 */
uint32_t ZcmSauglMssduTimestamp_encoded_size(const ZcmSauglMssduTimestamp* p);
uint32_t ZcmSauglMssduTimestamp_struct_size(void);
uint32_t ZcmSauglMssduTimestamp_num_fields(void);
int      ZcmSauglMssduTimestamp_get_field(const ZcmSauglMssduTimestamp* p, uint32_t i, zcm_field_t* f);
const zcm_type_info_t* ZcmSauglMssduTimestamp_get_type_info(void);

// ZCM support functions. Users should not call these
int64_t  __ZcmSauglMssduTimestamp_get_hash(void);
uint64_t __ZcmSauglMssduTimestamp_hash_recursive(const __zcm_hash_ptr* p);
int      __ZcmSauglMssduTimestamp_encode_array(void* buf, uint32_t offset, uint32_t maxlen, const ZcmSauglMssduTimestamp* p, uint32_t elements);
int      __ZcmSauglMssduTimestamp_decode_array(const void* buf, uint32_t offset, uint32_t maxlen, ZcmSauglMssduTimestamp* p, uint32_t elements);
int      __ZcmSauglMssduTimestamp_decode_array_cleanup(ZcmSauglMssduTimestamp* p, uint32_t elements);
uint32_t __ZcmSauglMssduTimestamp_encoded_array_size(const ZcmSauglMssduTimestamp* p, uint32_t elements);
uint32_t __ZcmSauglMssduTimestamp_clone_array(const ZcmSauglMssduTimestamp* p, ZcmSauglMssduTimestamp* q, uint32_t elements);

#ifdef __cplusplus
}
#endif

#endif
