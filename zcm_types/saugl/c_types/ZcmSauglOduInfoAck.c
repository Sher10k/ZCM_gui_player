// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by zcm-gen

#include <string.h>
#include "ZcmSauglOduInfoAck.h"

static int __ZcmSauglOduInfoAck_hash_computed;
static uint64_t __ZcmSauglOduInfoAck_hash;

uint64_t __ZcmSauglOduInfoAck_hash_recursive(const __zcm_hash_ptr* p)
{
    const __zcm_hash_ptr* fp;
    for (fp = p; fp != NULL; fp = fp->parent)
        if (fp->v == __ZcmSauglOduInfoAck_get_hash)
            return 0;

    __zcm_hash_ptr cp;
    cp.parent =  p;
    cp.v = (void*)__ZcmSauglOduInfoAck_get_hash;
    (void) cp;

    uint64_t hash = (uint64_t)0x9b29b194801ccc14LL
         + __ZcmService_hash_recursive(&cp)
         + __int16_t_hash_recursive(&cp)
        ;

    return (hash<<1) + ((hash>>63)&1);
}

int64_t __ZcmSauglOduInfoAck_get_hash(void)
{
    if (!__ZcmSauglOduInfoAck_hash_computed) {
        __ZcmSauglOduInfoAck_hash = (int64_t)__ZcmSauglOduInfoAck_hash_recursive(NULL);
        __ZcmSauglOduInfoAck_hash_computed = 1;
    }

    return __ZcmSauglOduInfoAck_hash;
}

int __ZcmSauglOduInfoAck_encode_array(void* buf, uint32_t offset, uint32_t maxlen, const ZcmSauglOduInfoAck* p, uint32_t elements)
{
    uint32_t pos = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        thislen = __ZcmService_encode_array(buf, offset + pos, maxlen - pos, &(p[element].service), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].counter), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int ZcmSauglOduInfoAck_encode(void* buf, uint32_t offset, uint32_t maxlen, const ZcmSauglOduInfoAck* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __ZcmSauglOduInfoAck_get_hash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    thislen = __ZcmSauglOduInfoAck_encode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

uint32_t __ZcmSauglOduInfoAck_encoded_array_size(const ZcmSauglOduInfoAck* p, uint32_t elements)
{
    uint32_t size = 0, element;
    for (element = 0; element < elements; ++element) {

        size += __ZcmService_encoded_array_size(&(p[element].service), 1);

        size += __int16_t_encoded_array_size(&(p[element].counter), 1);

    }
    return size;
}

uint32_t ZcmSauglOduInfoAck_encoded_size(const ZcmSauglOduInfoAck* p)
{
    return 8 + __ZcmSauglOduInfoAck_encoded_array_size(p, 1);
}

uint32_t ZcmSauglOduInfoAck_struct_size(void)
{
    return sizeof(ZcmSauglOduInfoAck);
}

uint32_t ZcmSauglOduInfoAck_num_fields(void)
{
    return 2;
}

int ZcmSauglOduInfoAck_get_field(const ZcmSauglOduInfoAck* p, uint32_t i, zcm_field_t* f)
{
    if (i >= ZcmSauglOduInfoAck_num_fields())
        return 1;
    
    switch (i) {
    
        case 0: {
            /* ZcmService */
            f->name = "service";
            f->type = ZCM_FIELD_USER_TYPE;
            f->typestr = "ZcmService";
            f->num_dim = 0;
            f->data = (void*) &p->service;
            return 0;
        }
        
        case 1: {
            f->name = "counter";
            f->type = ZCM_FIELD_INT16_T;
            f->typestr = "int16_t";
            f->num_dim = 0;
            f->data = (void*) &p->counter;
            return 0;
        }
        
        default:
            return 1;
    }
}

const zcm_type_info_t* ZcmSauglOduInfoAck_get_type_info(void)
{
    static int init = 0;
    static zcm_type_info_t typeinfo;
    if (!init) {
        typeinfo.encode         = (zcm_encode_t) ZcmSauglOduInfoAck_encode;
        typeinfo.decode         = (zcm_decode_t) ZcmSauglOduInfoAck_decode;
        typeinfo.decode_cleanup = (zcm_decode_cleanup_t) ZcmSauglOduInfoAck_decode_cleanup;
        typeinfo.encoded_size   = (zcm_encoded_size_t) ZcmSauglOduInfoAck_encoded_size;
        typeinfo.struct_size    = (zcm_struct_size_t)  ZcmSauglOduInfoAck_struct_size;
        typeinfo.num_fields     = (zcm_num_fields_t) ZcmSauglOduInfoAck_num_fields;
        typeinfo.get_field      = (zcm_get_field_t) ZcmSauglOduInfoAck_get_field;
        typeinfo.get_hash       = (zcm_get_hash_t) __ZcmSauglOduInfoAck_get_hash;
    }
    
    return &typeinfo;
}
int __ZcmSauglOduInfoAck_decode_array(const void* buf, uint32_t offset, uint32_t maxlen, ZcmSauglOduInfoAck* p, uint32_t elements)
{
    uint32_t pos = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        thislen = __ZcmService_decode_array(buf, offset + pos, maxlen - pos, &(p[element].service), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].counter), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int __ZcmSauglOduInfoAck_decode_array_cleanup(ZcmSauglOduInfoAck* p, uint32_t elements)
{
    uint32_t element;
    for (element = 0; element < elements; ++element) {

        __ZcmService_decode_array_cleanup(&(p[element].service), 1);

        __int16_t_decode_array_cleanup(&(p[element].counter), 1);

    }
    return 0;
}

int ZcmSauglOduInfoAck_decode(const void* buf, uint32_t offset, uint32_t maxlen, ZcmSauglOduInfoAck* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __ZcmSauglOduInfoAck_get_hash();

    int64_t this_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (this_hash != hash) return -1;

    thislen = __ZcmSauglOduInfoAck_decode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int ZcmSauglOduInfoAck_decode_cleanup(ZcmSauglOduInfoAck* p)
{
    return __ZcmSauglOduInfoAck_decode_array_cleanup(p, 1);
}

uint32_t __ZcmSauglOduInfoAck_clone_array(const ZcmSauglOduInfoAck* p, ZcmSauglOduInfoAck* q, uint32_t elements)
{
    uint32_t n = 0, element;
    for (element = 0; element < elements; ++element) {

        n += __ZcmService_clone_array(&(p[element].service), &(q[element].service), 1);

        n += __int16_t_clone_array(&(p[element].counter), &(q[element].counter), 1);

    }
    return n;
}

ZcmSauglOduInfoAck* ZcmSauglOduInfoAck_copy(const ZcmSauglOduInfoAck* p)
{
    ZcmSauglOduInfoAck* q = (ZcmSauglOduInfoAck*) malloc(sizeof(ZcmSauglOduInfoAck));
    __ZcmSauglOduInfoAck_clone_array(p, q, 1);
    return q;
}

void ZcmSauglOduInfoAck_destroy(ZcmSauglOduInfoAck* p)
{
    __ZcmSauglOduInfoAck_decode_array_cleanup(p, 1);
    free(p);
}

int ZcmSauglOduInfoAck_publish(zcm_t* zcm, const char* channel, const ZcmSauglOduInfoAck* p)
{
      uint32_t max_data_size = ZcmSauglOduInfoAck_encoded_size (p);
      uint8_t* buf = (uint8_t*) malloc (max_data_size);
      if (!buf) return -1;
      int data_size = ZcmSauglOduInfoAck_encode (buf, 0, max_data_size, p);
      if (data_size < 0) {
          free (buf);
          return data_size;
      }
      int status = zcm_publish (zcm, channel, buf, (uint32_t)data_size);
      free (buf);
      return status;
}

struct _ZcmSauglOduInfoAck_subscription_t {
    ZcmSauglOduInfoAck_handler_t user_handler;
    void* userdata;
    zcm_sub_t* z_sub;
};
static
void ZcmSauglOduInfoAck_handler_stub (const zcm_recv_buf_t* rbuf,
                            const char* channel, void* userdata)
{
    int status;
    ZcmSauglOduInfoAck p;
    memset(&p, 0, sizeof(ZcmSauglOduInfoAck));
    status = ZcmSauglOduInfoAck_decode (rbuf->data, 0, rbuf->data_size, &p);
    if (status < 0) {
        fprintf (stderr, "error %d decoding ZcmSauglOduInfoAck!!!\n", status);
        return;
    }

    ZcmSauglOduInfoAck_subscription_t* h = (ZcmSauglOduInfoAck_subscription_t*) userdata;
    h->user_handler (rbuf, channel, &p, h->userdata);

    ZcmSauglOduInfoAck_decode_cleanup (&p);
}

ZcmSauglOduInfoAck_subscription_t* ZcmSauglOduInfoAck_subscribe (zcm_t* zcm,
                    const char* channel,
                    ZcmSauglOduInfoAck_handler_t f, void* userdata)
{
    ZcmSauglOduInfoAck_subscription_t* n = (ZcmSauglOduInfoAck_subscription_t*)
                       malloc(sizeof(ZcmSauglOduInfoAck_subscription_t));
    n->user_handler = f;
    n->userdata = userdata;
    n->z_sub = zcm_subscribe (zcm, channel,
                              ZcmSauglOduInfoAck_handler_stub, n);
    if (n->z_sub == NULL) {
        fprintf (stderr,"couldn't reg ZcmSauglOduInfoAck ZCM handler!\n");
        free (n);
        return NULL;
    }
    return n;
}

int ZcmSauglOduInfoAck_unsubscribe(zcm_t* zcm, ZcmSauglOduInfoAck_subscription_t* hid)
{
    int status = zcm_unsubscribe (zcm, hid->z_sub);
    if (0 != status) {
        fprintf(stderr,
           "couldn't unsubscribe ZcmSauglOduInfoAck_handler %p!\n", hid);
        return -1;
    }
    free (hid);
    return 0;
}

