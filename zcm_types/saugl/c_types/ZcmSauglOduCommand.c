// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by zcm-gen

#include <string.h>
#include "ZcmSauglOduCommand.h"

static int __ZcmSauglOduCommand_hash_computed;
static uint64_t __ZcmSauglOduCommand_hash;

uint64_t __ZcmSauglOduCommand_hash_recursive(const __zcm_hash_ptr* p)
{
    const __zcm_hash_ptr* fp;
    for (fp = p; fp != NULL; fp = fp->parent)
        if (fp->v == __ZcmSauglOduCommand_get_hash)
            return 0;

    __zcm_hash_ptr cp;
    cp.parent =  p;
    cp.v = (void*)__ZcmSauglOduCommand_get_hash;
    (void) cp;

    uint64_t hash = (uint64_t)0x32cab9544265f203LL
         + __boolean_hash_recursive(&cp)
         + __boolean_hash_recursive(&cp)
        ;

    return (hash<<1) + ((hash>>63)&1);
}

int64_t __ZcmSauglOduCommand_get_hash(void)
{
    if (!__ZcmSauglOduCommand_hash_computed) {
        __ZcmSauglOduCommand_hash = (int64_t)__ZcmSauglOduCommand_hash_recursive(NULL);
        __ZcmSauglOduCommand_hash_computed = 1;
    }

    return __ZcmSauglOduCommand_hash;
}

int __ZcmSauglOduCommand_encode_array(void* buf, uint32_t offset, uint32_t maxlen, const ZcmSauglOduCommand* p, uint32_t elements)
{
    uint32_t pos = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        thislen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &(p[element].is_signal), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &(p[element].is_brake), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int ZcmSauglOduCommand_encode(void* buf, uint32_t offset, uint32_t maxlen, const ZcmSauglOduCommand* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __ZcmSauglOduCommand_get_hash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    thislen = __ZcmSauglOduCommand_encode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

uint32_t __ZcmSauglOduCommand_encoded_array_size(const ZcmSauglOduCommand* p, uint32_t elements)
{
    uint32_t size = 0, element;
    for (element = 0; element < elements; ++element) {

        size += __boolean_encoded_array_size(&(p[element].is_signal), 1);

        size += __boolean_encoded_array_size(&(p[element].is_brake), 1);

    }
    return size;
}

uint32_t ZcmSauglOduCommand_encoded_size(const ZcmSauglOduCommand* p)
{
    return 8 + __ZcmSauglOduCommand_encoded_array_size(p, 1);
}

uint32_t ZcmSauglOduCommand_struct_size(void)
{
    return sizeof(ZcmSauglOduCommand);
}

uint32_t ZcmSauglOduCommand_num_fields(void)
{
    return 2;
}

int ZcmSauglOduCommand_get_field(const ZcmSauglOduCommand* p, uint32_t i, zcm_field_t* f)
{
    if (i >= ZcmSauglOduCommand_num_fields())
        return 1;
    
    switch (i) {
    
        case 0: {
            f->name = "is_signal";
            f->type = ZCM_FIELD_BOOLEAN;
            f->typestr = "boolean";
            f->num_dim = 0;
            f->data = (void*) &p->is_signal;
            return 0;
        }
        
        case 1: {
            f->name = "is_brake";
            f->type = ZCM_FIELD_BOOLEAN;
            f->typestr = "boolean";
            f->num_dim = 0;
            f->data = (void*) &p->is_brake;
            return 0;
        }
        
        default:
            return 1;
    }
}

const zcm_type_info_t* ZcmSauglOduCommand_get_type_info(void)
{
    static int init = 0;
    static zcm_type_info_t typeinfo;
    if (!init) {
        typeinfo.encode         = (zcm_encode_t) ZcmSauglOduCommand_encode;
        typeinfo.decode         = (zcm_decode_t) ZcmSauglOduCommand_decode;
        typeinfo.decode_cleanup = (zcm_decode_cleanup_t) ZcmSauglOduCommand_decode_cleanup;
        typeinfo.encoded_size   = (zcm_encoded_size_t) ZcmSauglOduCommand_encoded_size;
        typeinfo.struct_size    = (zcm_struct_size_t)  ZcmSauglOduCommand_struct_size;
        typeinfo.num_fields     = (zcm_num_fields_t) ZcmSauglOduCommand_num_fields;
        typeinfo.get_field      = (zcm_get_field_t) ZcmSauglOduCommand_get_field;
        typeinfo.get_hash       = (zcm_get_hash_t) __ZcmSauglOduCommand_get_hash;
    }
    
    return &typeinfo;
}
int __ZcmSauglOduCommand_decode_array(const void* buf, uint32_t offset, uint32_t maxlen, ZcmSauglOduCommand* p, uint32_t elements)
{
    uint32_t pos = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        thislen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &(p[element].is_signal), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &(p[element].is_brake), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int __ZcmSauglOduCommand_decode_array_cleanup(ZcmSauglOduCommand* p, uint32_t elements)
{
    uint32_t element;
    for (element = 0; element < elements; ++element) {

        __boolean_decode_array_cleanup(&(p[element].is_signal), 1);

        __boolean_decode_array_cleanup(&(p[element].is_brake), 1);

    }
    return 0;
}

int ZcmSauglOduCommand_decode(const void* buf, uint32_t offset, uint32_t maxlen, ZcmSauglOduCommand* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __ZcmSauglOduCommand_get_hash();

    int64_t this_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (this_hash != hash) return -1;

    thislen = __ZcmSauglOduCommand_decode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int ZcmSauglOduCommand_decode_cleanup(ZcmSauglOduCommand* p)
{
    return __ZcmSauglOduCommand_decode_array_cleanup(p, 1);
}

uint32_t __ZcmSauglOduCommand_clone_array(const ZcmSauglOduCommand* p, ZcmSauglOduCommand* q, uint32_t elements)
{
    uint32_t n = 0, element;
    for (element = 0; element < elements; ++element) {

        n += __boolean_clone_array(&(p[element].is_signal), &(q[element].is_signal), 1);

        n += __boolean_clone_array(&(p[element].is_brake), &(q[element].is_brake), 1);

    }
    return n;
}

ZcmSauglOduCommand* ZcmSauglOduCommand_copy(const ZcmSauglOduCommand* p)
{
    ZcmSauglOduCommand* q = (ZcmSauglOduCommand*) malloc(sizeof(ZcmSauglOduCommand));
    __ZcmSauglOduCommand_clone_array(p, q, 1);
    return q;
}

void ZcmSauglOduCommand_destroy(ZcmSauglOduCommand* p)
{
    __ZcmSauglOduCommand_decode_array_cleanup(p, 1);
    free(p);
}

int ZcmSauglOduCommand_publish(zcm_t* zcm, const char* channel, const ZcmSauglOduCommand* p)
{
      uint32_t max_data_size = ZcmSauglOduCommand_encoded_size (p);
      uint8_t* buf = (uint8_t*) malloc (max_data_size);
      if (!buf) return -1;
      int data_size = ZcmSauglOduCommand_encode (buf, 0, max_data_size, p);
      if (data_size < 0) {
          free (buf);
          return data_size;
      }
      int status = zcm_publish (zcm, channel, buf, (uint32_t)data_size);
      free (buf);
      return status;
}

struct _ZcmSauglOduCommand_subscription_t {
    ZcmSauglOduCommand_handler_t user_handler;
    void* userdata;
    zcm_sub_t* z_sub;
};
static
void ZcmSauglOduCommand_handler_stub (const zcm_recv_buf_t* rbuf,
                            const char* channel, void* userdata)
{
    int status;
    ZcmSauglOduCommand p;
    memset(&p, 0, sizeof(ZcmSauglOduCommand));
    status = ZcmSauglOduCommand_decode (rbuf->data, 0, rbuf->data_size, &p);
    if (status < 0) {
        fprintf (stderr, "error %d decoding ZcmSauglOduCommand!!!\n", status);
        return;
    }

    ZcmSauglOduCommand_subscription_t* h = (ZcmSauglOduCommand_subscription_t*) userdata;
    h->user_handler (rbuf, channel, &p, h->userdata);

    ZcmSauglOduCommand_decode_cleanup (&p);
}

ZcmSauglOduCommand_subscription_t* ZcmSauglOduCommand_subscribe (zcm_t* zcm,
                    const char* channel,
                    ZcmSauglOduCommand_handler_t f, void* userdata)
{
    ZcmSauglOduCommand_subscription_t* n = (ZcmSauglOduCommand_subscription_t*)
                       malloc(sizeof(ZcmSauglOduCommand_subscription_t));
    n->user_handler = f;
    n->userdata = userdata;
    n->z_sub = zcm_subscribe (zcm, channel,
                              ZcmSauglOduCommand_handler_stub, n);
    if (n->z_sub == NULL) {
        fprintf (stderr,"couldn't reg ZcmSauglOduCommand ZCM handler!\n");
        free (n);
        return NULL;
    }
    return n;
}

int ZcmSauglOduCommand_unsubscribe(zcm_t* zcm, ZcmSauglOduCommand_subscription_t* hid)
{
    int status = zcm_unsubscribe (zcm, hid->z_sub);
    if (0 != status) {
        fprintf(stderr,
           "couldn't unsubscribe ZcmSauglOduCommand_handler %p!\n", hid);
        return -1;
    }
    free (hid);
    return 0;
}

