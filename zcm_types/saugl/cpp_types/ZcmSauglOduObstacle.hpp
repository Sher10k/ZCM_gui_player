/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by zcm-gen
 **/

#include <zcm/zcm_coretypes.h>

#ifndef __ZcmSauglOduObstacle_hpp__
#define __ZcmSauglOduObstacle_hpp__



/**
 * @mainpage Сообщения взаимодействия с системой САУ-ГЛ (Система автоматического управления горочного локомотива)
 *
 * @struct ZcmSauglOduObstacle
 * @brief Информация блока обнаружения препятствий о препятствии
 * @var ZcmSauglOduObstacle::is_obstacle
 * @brief - признак обнаружения препятствия
 * @var ZcmSauglOduObstacle::allowed_speed
 * @brief - текущая заданная скорость, от 0 до 25, 255 (0xff) - не установлена, км/ч
 * @var ZcmSauglOduObstacle::target_speed
 * @brief - целевая заданная скорость, от 0 до 25, 255 (0xff) - не установлена, км/ч
 * @var ZcmSauglOduObstacle::target_distance
 * @brief - расстояние до достижения целевой заданной скорости, от 0 до 254, 255 (0xff) - не установлено, м
 * @var ZcmSauglOduObstacle::SPEED_NOT_SET
 * @brief - константа значение скорости не установлено
 * @var ZcmSauglOduObstacle::DISTANCE_NOT_SET
 * @brief - константа значение расстояния не установлено
 *
 */
class ZcmSauglOduObstacle
{
    public:
        int8_t     is_obstacle;

        int16_t    allowed_speed;

        int16_t    target_speed;

        int16_t    target_distance;

    public:
        #if __cplusplus > 199711L /* if c++11 */
        static constexpr int16_t  SPEED_NOT_SET = 0xff;
        static constexpr int16_t  DISTANCE_NOT_SET = 0xff;
        #else
        static const     int16_t  SPEED_NOT_SET = 0xff;
        static const     int16_t  DISTANCE_NOT_SET = 0xff;
        #endif

    public:
        /**
         * Destructs a message properly if anything inherits from it
        */
        virtual ~ZcmSauglOduObstacle() {}

        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void* buf, uint32_t offset, uint32_t maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline uint32_t getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to reqad while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void* buf, uint32_t offset, uint32_t maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "ZcmSauglOduObstacle"
         */
        inline static const char* getTypeName();

        // ZCM support functions. Users should not call these
        inline int      _encodeNoHash(void* buf, uint32_t offset, uint32_t maxlen) const;
        inline uint32_t _getEncodedSizeNoHash() const;
        inline int      _decodeNoHash(const void* buf, uint32_t offset, uint32_t maxlen);
        inline static uint64_t _computeHash(const __zcm_hash_ptr* p);
};

int ZcmSauglOduObstacle::encode(void* buf, uint32_t offset, uint32_t maxlen) const
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = (int64_t)getHash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int ZcmSauglOduObstacle::decode(const void* buf, uint32_t offset, uint32_t maxlen)
{
    uint32_t pos = 0;
    int thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

uint32_t ZcmSauglOduObstacle::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t ZcmSauglOduObstacle::getHash()
{
    static int64_t hash = _computeHash(NULL);
    return hash;
}

const char* ZcmSauglOduObstacle::getTypeName()
{
    return "ZcmSauglOduObstacle";
}

int ZcmSauglOduObstacle::_encodeNoHash(void* buf, uint32_t offset, uint32_t maxlen) const
{
    uint32_t pos = 0;
    int thislen;

    thislen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &this->is_obstacle, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->allowed_speed, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->target_speed, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->target_distance, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int ZcmSauglOduObstacle::_decodeNoHash(const void* buf, uint32_t offset, uint32_t maxlen)
{
    uint32_t pos = 0;
    int thislen;

    thislen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &this->is_obstacle, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->allowed_speed, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->target_speed, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->target_distance, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    return pos;
}

uint32_t ZcmSauglOduObstacle::_getEncodedSizeNoHash() const
{
    uint32_t enc_size = 0;
    enc_size += __boolean_encoded_array_size(NULL, 1);
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    return enc_size;
}

uint64_t ZcmSauglOduObstacle::_computeHash(const __zcm_hash_ptr*)
{
    uint64_t hash = (uint64_t)0x701891d3ef50655cLL;
    return (hash<<1) + ((hash>>63)&1);
}

#endif
