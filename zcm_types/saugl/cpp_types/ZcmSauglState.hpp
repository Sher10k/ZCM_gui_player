/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by zcm-gen
 **/

#include <zcm/zcm_coretypes.h>

#ifndef __ZcmSauglState_hpp__
#define __ZcmSauglState_hpp__

#include "ZcmService.hpp"


/**
 * @struct ZcmSauglState
 * @brief Состояние САУ-ГЛ
 * @var ZcmSauglState::power
 * @brief - Значение контроллера тяги, от -128 до 127
 * @var ZcmSauglState::brake
 * @brief - Значение позиции тормоза, от 0 до 4
 * @var ZcmSauglState::reversor
 * @brief - Фактическое положение реверсора
 * @var ZcmSauglState::mode
 * @brief - Режим управления
 * @var ZcmSauglState::mals_operation
 * @brief - Выполняемая операция МАЛС
 * @var ZcmSauglState::pressure
 * @brief - Давление в ТЦ, мПа* 100
 * @var ZcmSauglState::speed
 * @brief - Фактическая скорость, км/ч
 * @var ZcmSauglState::hold_speed
 * @brief - Поддерживаемая скорость, км/ч
 * @var ZcmSauglState::REVERSOR_NEUTRAL
 * @brief - Константа: Положение реверсора - нейтраль
 * @var ZcmSauglState::REVERSOR_FORWARD
 * @brief - Константа: Положение реверсора - вперед
 * @var ZcmSauglState::REVERSOR_BACKWARD
 * @brief - Константа: Положение реверсора - назад
 * @var ZcmSauglState::MODE_HAND
 * @brief - Константа: Режим управления - ручной (из кабины)
 * @var ZcmSauglState::MODE_DISTANCE
 * @brief - Константа: Режим управления - дистанционный
 * @var ZcmSauglState::MODE_AUTO_MALS
 * @brief - Константа: Режим управления - автоматический (МАЛС)
 * @var ZcmSauglState::MODE_AUTO_MSR32
 * @brief - Константа: Режим управления - автоматический (MSR-32)
 * @var ZcmSauglState::MODE_AUTO_WAIT_SWITCH
 * @brief - Константа: Режим управления - автоматический (ожидание переключения управляющей системы)
 * @var ZcmSauglState::MODE_AUTO_WAIT_HAND
 * @brief - Константа: Режим управления - автоматический ()
 * @var ZcmSauglState::MALS_OPERATION_STANDBY
 * @brief - Константа: Выполняемая операция МАЛС: стоянка
 * @var ZcmSauglState::MALS_OPERATION_ROUTE
 * @brief - Константа: Выполняемая операция МАЛС: движение до конца маршрута (в тупик или до светофора)
 * @var ZcmSauglState::MALS_OPERATION_OCCUPIED_TRACK
 * @brief - Константа: Выполняемая операция МАЛС: движение на занятый путь
 * @var ZcmSauglState::MALS_OPERATION_WAIT_CONNECT
 * @brief - Константа: Выполняемая операция МАЛС: ожидание сцепки
 * @var ZcmSauglState::MALS_OPERATION_EXTEND_CONNECT
 * @brief - Константа: Выполняемая операция МАЛС: растяжка автосцепки
 *
 */
class ZcmSauglState
{
    public:
        ZcmService service;

        int8_t     power;

        int16_t    brake;

        int8_t     reversor;

        int8_t     mode;

        int8_t     mals_operation;

        int16_t    pressure;

        int16_t    speed;

        int16_t    hold_speed;

    public:
        #if __cplusplus > 199711L /* if c++11 */
        static constexpr int8_t   REVERSOR_NEUTRAL = 0;
        static constexpr int8_t   REVERSOR_FORWARD = 1;
        static constexpr int8_t   REVERSOR_BACKWARD = 2;
        static constexpr int8_t   MODE_HAND = 0;
        static constexpr int8_t   MODE_DISTANCE = 1;
        static constexpr int8_t   MODE_AUTO_MALS = 2;
        static constexpr int8_t   MODE_AUTO_MSR32 = 3;
        static constexpr int8_t   MODE_AUTO_WAIT_SWITCH = 4;
        static constexpr int8_t   MODE_AUTO_WAIT_HAND = 5;
        static constexpr int8_t   MALS_OPERATION_STANDBY = 0;
        static constexpr int8_t   MALS_OPERATION_ROUTE = 1;
        static constexpr int8_t   MALS_OPERATION_OCCUPIED_TRACK = 2;
        static constexpr int8_t   MALS_OPERATION_WAIT_CONNECT = 3;
        static constexpr int8_t   MALS_OPERATION_EXTEND_CONNECT = 4;
        #else
        static const     int8_t   REVERSOR_NEUTRAL = 0;
        static const     int8_t   REVERSOR_FORWARD = 1;
        static const     int8_t   REVERSOR_BACKWARD = 2;
        static const     int8_t   MODE_HAND = 0;
        static const     int8_t   MODE_DISTANCE = 1;
        static const     int8_t   MODE_AUTO_MALS = 2;
        static const     int8_t   MODE_AUTO_MSR32 = 3;
        static const     int8_t   MODE_AUTO_WAIT_SWITCH = 4;
        static const     int8_t   MODE_AUTO_WAIT_HAND = 5;
        static const     int8_t   MALS_OPERATION_STANDBY = 0;
        static const     int8_t   MALS_OPERATION_ROUTE = 1;
        static const     int8_t   MALS_OPERATION_OCCUPIED_TRACK = 2;
        static const     int8_t   MALS_OPERATION_WAIT_CONNECT = 3;
        static const     int8_t   MALS_OPERATION_EXTEND_CONNECT = 4;
        #endif

    public:
        /**
         * Destructs a message properly if anything inherits from it
        */
        virtual ~ZcmSauglState() {}

        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void* buf, uint32_t offset, uint32_t maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline uint32_t getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to reqad while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void* buf, uint32_t offset, uint32_t maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "ZcmSauglState"
         */
        inline static const char* getTypeName();

        // ZCM support functions. Users should not call these
        inline int      _encodeNoHash(void* buf, uint32_t offset, uint32_t maxlen) const;
        inline uint32_t _getEncodedSizeNoHash() const;
        inline int      _decodeNoHash(const void* buf, uint32_t offset, uint32_t maxlen);
        inline static uint64_t _computeHash(const __zcm_hash_ptr* p);
};

int ZcmSauglState::encode(void* buf, uint32_t offset, uint32_t maxlen) const
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = (int64_t)getHash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int ZcmSauglState::decode(const void* buf, uint32_t offset, uint32_t maxlen)
{
    uint32_t pos = 0;
    int thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

uint32_t ZcmSauglState::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t ZcmSauglState::getHash()
{
    static int64_t hash = _computeHash(NULL);
    return hash;
}

const char* ZcmSauglState::getTypeName()
{
    return "ZcmSauglState";
}

int ZcmSauglState::_encodeNoHash(void* buf, uint32_t offset, uint32_t maxlen) const
{
    uint32_t pos = 0;
    int thislen;

    thislen = this->service._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->power, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->brake, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->reversor, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->mode, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->mals_operation, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->pressure, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->speed, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->hold_speed, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int ZcmSauglState::_decodeNoHash(const void* buf, uint32_t offset, uint32_t maxlen)
{
    uint32_t pos = 0;
    int thislen;

    thislen = this->service._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->power, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->brake, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->reversor, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->mode, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->mals_operation, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->pressure, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->speed, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->hold_speed, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    return pos;
}

uint32_t ZcmSauglState::_getEncodedSizeNoHash() const
{
    uint32_t enc_size = 0;
    enc_size += this->service._getEncodedSizeNoHash();
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    return enc_size;
}

uint64_t ZcmSauglState::_computeHash(const __zcm_hash_ptr* p)
{
    const __zcm_hash_ptr* fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == ZcmSauglState::getHash)
            return 0;
    const __zcm_hash_ptr cp = { p, (void*)ZcmSauglState::getHash };

    uint64_t hash = (uint64_t)0x1cb213138e185178LL +
         ZcmService::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

#endif
