"""ZCM type definitions
This file automatically generated by zcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class ZcmSauglOduCar(object):
    __slots__ = ["is_car", "distance_capote", "distance_cabin"]

    DISTANCE_NOT_SET = 0xfff

    def __init__(self):
        self.is_car = False
        self.distance_capote = 0
        self.distance_cabin = 0

    def encode(self):
        buf = BytesIO()
        buf.write(ZcmSauglOduCar._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">bii", self.is_car, self.distance_capote, self.distance_cabin))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != ZcmSauglOduCar._get_packed_fingerprint():
            raise ValueError("Decode error")
        return ZcmSauglOduCar._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = ZcmSauglOduCar()
        self.is_car = bool(struct.unpack('b', buf.read(1))[0])
        self.distance_capote, self.distance_cabin = struct.unpack(">ii", buf.read(8))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if ZcmSauglOduCar in parents: return 0
        tmphash = (0xa2dd1748b4c0ba79) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + ((tmphash>>63)&0x1)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if ZcmSauglOduCar._packed_fingerprint is None:
            ZcmSauglOduCar._packed_fingerprint = struct.pack(">Q", ZcmSauglOduCar._get_hash_recursive([]))
        return ZcmSauglOduCar._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

