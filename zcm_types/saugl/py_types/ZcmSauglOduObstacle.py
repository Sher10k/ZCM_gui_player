"""ZCM type definitions
This file automatically generated by zcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class ZcmSauglOduObstacle(object):
    __slots__ = ["is_obstacle", "allowed_speed", "target_speed", "target_distance"]

    SPEED_NOT_SET = 0xff
    DISTANCE_NOT_SET = 0xff

    def __init__(self):
        self.is_obstacle = False
        self.allowed_speed = 0
        self.target_speed = 0
        self.target_distance = 0

    def encode(self):
        buf = BytesIO()
        buf.write(ZcmSauglOduObstacle._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">bhhh", self.is_obstacle, self.allowed_speed, self.target_speed, self.target_distance))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != ZcmSauglOduObstacle._get_packed_fingerprint():
            raise ValueError("Decode error")
        return ZcmSauglOduObstacle._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = ZcmSauglOduObstacle()
        self.is_obstacle = bool(struct.unpack('b', buf.read(1))[0])
        self.allowed_speed, self.target_speed, self.target_distance = struct.unpack(">hhh", buf.read(6))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if ZcmSauglOduObstacle in parents: return 0
        tmphash = (0x701891d3ef50655c) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + ((tmphash>>63)&0x1)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if ZcmSauglOduObstacle._packed_fingerprint is None:
            ZcmSauglOduObstacle._packed_fingerprint = struct.pack(">Q", ZcmSauglOduObstacle._get_hash_recursive([]))
        return ZcmSauglOduObstacle._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

