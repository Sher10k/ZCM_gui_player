// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by zcm-gen

#include <string.h>
#include "ZcmDisparityMap.h"

static int __ZcmDisparityMap_hash_computed;
static uint64_t __ZcmDisparityMap_hash;

uint64_t __ZcmDisparityMap_hash_recursive(const __zcm_hash_ptr* p)
{
    const __zcm_hash_ptr* fp;
    for (fp = p; fp != NULL; fp = fp->parent)
        if (fp->v == __ZcmDisparityMap_get_hash)
            return 0;

    __zcm_hash_ptr cp;
    cp.parent =  p;
    cp.v = (void*)__ZcmDisparityMap_get_hash;
    (void) cp;

    uint64_t hash = (uint64_t)0x3aa5ec60f15f8df2LL
         + __ZcmService_hash_recursive(&cp)
         + __ZcmCameraBaslerJpegFrame_hash_recursive(&cp)
         + __int32_t_hash_recursive(&cp)
         + __int32_t_hash_recursive(&cp)
         + __int32_t_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
         + __int32_t_hash_recursive(&cp)
         + __int16_t_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
        ;

    return (hash<<1) + ((hash>>63)&1);
}

int64_t __ZcmDisparityMap_get_hash(void)
{
    if (!__ZcmDisparityMap_hash_computed) {
        __ZcmDisparityMap_hash = (int64_t)__ZcmDisparityMap_hash_recursive(NULL);
        __ZcmDisparityMap_hash_computed = 1;
    }

    return __ZcmDisparityMap_hash;
}

int __ZcmDisparityMap_encode_array(void* buf, uint32_t offset, uint32_t maxlen, const ZcmDisparityMap* p, uint32_t elements)
{
    uint32_t pos = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        thislen = __ZcmService_encode_array(buf, offset + pos, maxlen - pos, &(p[element].service), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __ZcmCameraBaslerJpegFrame_encode_array(buf, offset + pos, maxlen - pos, &(p[element].jpeg_frame), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].height), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].width), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].size), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].scale), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, p[element].padding, 4);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, p[element].disp, p[element].size);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, p[element].Q, 16);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, p[element].R, 3);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, p[element].T, 3);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int ZcmDisparityMap_encode(void* buf, uint32_t offset, uint32_t maxlen, const ZcmDisparityMap* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __ZcmDisparityMap_get_hash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    thislen = __ZcmDisparityMap_encode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

uint32_t __ZcmDisparityMap_encoded_array_size(const ZcmDisparityMap* p, uint32_t elements)
{
    uint32_t size = 0, element;
    for (element = 0; element < elements; ++element) {

        size += __ZcmService_encoded_array_size(&(p[element].service), 1);

        size += __ZcmCameraBaslerJpegFrame_encoded_array_size(&(p[element].jpeg_frame), 1);

        size += __int32_t_encoded_array_size(&(p[element].height), 1);

        size += __int32_t_encoded_array_size(&(p[element].width), 1);

        size += __int32_t_encoded_array_size(&(p[element].size), 1);

        size += __double_encoded_array_size(&(p[element].scale), 1);

        size += __int32_t_encoded_array_size(p[element].padding, 4);

        size += __int16_t_encoded_array_size(p[element].disp, p[element].size);

        size += __double_encoded_array_size(p[element].Q, 16);

        size += __double_encoded_array_size(p[element].R, 3);

        size += __double_encoded_array_size(p[element].T, 3);

    }
    return size;
}

uint32_t ZcmDisparityMap_encoded_size(const ZcmDisparityMap* p)
{
    return 8 + __ZcmDisparityMap_encoded_array_size(p, 1);
}

uint32_t ZcmDisparityMap_struct_size(void)
{
    return sizeof(ZcmDisparityMap);
}

uint32_t ZcmDisparityMap_num_fields(void)
{
    return 11;
}

int ZcmDisparityMap_get_field(const ZcmDisparityMap* p, uint32_t i, zcm_field_t* f)
{
    if (i >= ZcmDisparityMap_num_fields())
        return 1;
    
    switch (i) {
    
        case 0: {
            /* ZcmService */
            f->name = "service";
            f->type = ZCM_FIELD_USER_TYPE;
            f->typestr = "ZcmService";
            f->num_dim = 0;
            f->data = (void*) &p->service;
            return 0;
        }
        
        case 1: {
            /* ZcmCameraBaslerJpegFrame */
            f->name = "jpeg_frame";
            f->type = ZCM_FIELD_USER_TYPE;
            f->typestr = "ZcmCameraBaslerJpegFrame";
            f->num_dim = 0;
            f->data = (void*) &p->jpeg_frame;
            return 0;
        }
        
        case 2: {
            f->name = "height";
            f->type = ZCM_FIELD_INT32_T;
            f->typestr = "int32_t";
            f->num_dim = 0;
            f->data = (void*) &p->height;
            return 0;
        }
        
        case 3: {
            f->name = "width";
            f->type = ZCM_FIELD_INT32_T;
            f->typestr = "int32_t";
            f->num_dim = 0;
            f->data = (void*) &p->width;
            return 0;
        }
        
        case 4: {
            f->name = "size";
            f->type = ZCM_FIELD_INT32_T;
            f->typestr = "int32_t";
            f->num_dim = 0;
            f->data = (void*) &p->size;
            return 0;
        }
        
        case 5: {
            f->name = "scale";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 0;
            f->data = (void*) &p->scale;
            return 0;
        }
        
        case 6: {
            f->name = "padding";
            f->type = ZCM_FIELD_INT32_T;
            f->typestr = "int32_t";
            f->num_dim = 1;
            f->dim_size[0] = 4;
            f->dim_is_variable[0] = 0;
            f->data = (void*) &p->padding;
            return 0;
        }
        
        case 7: {
            f->name = "disp";
            f->type = ZCM_FIELD_INT16_T;
            f->typestr = "int16_t";
            f->num_dim = 1;
            f->dim_size[0] = p->size;
            f->dim_is_variable[0] = 1;
            f->data = (void*) &p->disp;
            return 0;
        }
        
        case 8: {
            f->name = "Q";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 1;
            f->dim_size[0] = 16;
            f->dim_is_variable[0] = 0;
            f->data = (void*) &p->Q;
            return 0;
        }
        
        case 9: {
            f->name = "R";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 1;
            f->dim_size[0] = 3;
            f->dim_is_variable[0] = 0;
            f->data = (void*) &p->R;
            return 0;
        }
        
        case 10: {
            f->name = "T";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 1;
            f->dim_size[0] = 3;
            f->dim_is_variable[0] = 0;
            f->data = (void*) &p->T;
            return 0;
        }
        
        default:
            return 1;
    }
}

const zcm_type_info_t* ZcmDisparityMap_get_type_info(void)
{
    static int init = 0;
    static zcm_type_info_t typeinfo;
    if (!init) {
        typeinfo.encode         = (zcm_encode_t) ZcmDisparityMap_encode;
        typeinfo.decode         = (zcm_decode_t) ZcmDisparityMap_decode;
        typeinfo.decode_cleanup = (zcm_decode_cleanup_t) ZcmDisparityMap_decode_cleanup;
        typeinfo.encoded_size   = (zcm_encoded_size_t) ZcmDisparityMap_encoded_size;
        typeinfo.struct_size    = (zcm_struct_size_t)  ZcmDisparityMap_struct_size;
        typeinfo.num_fields     = (zcm_num_fields_t) ZcmDisparityMap_num_fields;
        typeinfo.get_field      = (zcm_get_field_t) ZcmDisparityMap_get_field;
        typeinfo.get_hash       = (zcm_get_hash_t) __ZcmDisparityMap_get_hash;
    }
    
    return &typeinfo;
}
int __ZcmDisparityMap_decode_array(const void* buf, uint32_t offset, uint32_t maxlen, ZcmDisparityMap* p, uint32_t elements)
{
    uint32_t pos = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        thislen = __ZcmService_decode_array(buf, offset + pos, maxlen - pos, &(p[element].service), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __ZcmCameraBaslerJpegFrame_decode_array(buf, offset + pos, maxlen - pos, &(p[element].jpeg_frame), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].height), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].width), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].size), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].scale), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, p[element].padding, 4);
        if (thislen < 0) return thislen; else pos += thislen;

        p[element].disp = (int16_t*) zcm_malloc(sizeof(int16_t) * p[element].size);
        thislen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, p[element].disp, p[element].size);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, p[element].Q, 16);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, p[element].R, 3);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, p[element].T, 3);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int __ZcmDisparityMap_decode_array_cleanup(ZcmDisparityMap* p, uint32_t elements)
{
    uint32_t element;
    for (element = 0; element < elements; ++element) {

        __ZcmService_decode_array_cleanup(&(p[element].service), 1);

        __ZcmCameraBaslerJpegFrame_decode_array_cleanup(&(p[element].jpeg_frame), 1);

        __int32_t_decode_array_cleanup(&(p[element].height), 1);

        __int32_t_decode_array_cleanup(&(p[element].width), 1);

        __int32_t_decode_array_cleanup(&(p[element].size), 1);

        __double_decode_array_cleanup(&(p[element].scale), 1);

        __int32_t_decode_array_cleanup(p[element].padding, 4);

        __int16_t_decode_array_cleanup(p[element].disp, p[element].size);
        if (p[element].disp) free(p[element].disp);

        __double_decode_array_cleanup(p[element].Q, 16);

        __double_decode_array_cleanup(p[element].R, 3);

        __double_decode_array_cleanup(p[element].T, 3);

    }
    return 0;
}

int ZcmDisparityMap_decode(const void* buf, uint32_t offset, uint32_t maxlen, ZcmDisparityMap* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __ZcmDisparityMap_get_hash();

    int64_t this_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (this_hash != hash) return -1;

    thislen = __ZcmDisparityMap_decode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int ZcmDisparityMap_decode_cleanup(ZcmDisparityMap* p)
{
    return __ZcmDisparityMap_decode_array_cleanup(p, 1);
}

uint32_t __ZcmDisparityMap_clone_array(const ZcmDisparityMap* p, ZcmDisparityMap* q, uint32_t elements)
{
    uint32_t n = 0, element;
    for (element = 0; element < elements; ++element) {

        n += __ZcmService_clone_array(&(p[element].service), &(q[element].service), 1);

        n += __ZcmCameraBaslerJpegFrame_clone_array(&(p[element].jpeg_frame), &(q[element].jpeg_frame), 1);

        n += __int32_t_clone_array(&(p[element].height), &(q[element].height), 1);

        n += __int32_t_clone_array(&(p[element].width), &(q[element].width), 1);

        n += __int32_t_clone_array(&(p[element].size), &(q[element].size), 1);

        n += __double_clone_array(&(p[element].scale), &(q[element].scale), 1);

        n += __int32_t_clone_array(p[element].padding, q[element].padding, 4);

        q[element].disp = (int16_t*) zcm_malloc(sizeof(int16_t) * q[element].size);
        n += __int16_t_clone_array(p[element].disp, q[element].disp, p[element].size);

        n += __double_clone_array(p[element].Q, q[element].Q, 16);

        n += __double_clone_array(p[element].R, q[element].R, 3);

        n += __double_clone_array(p[element].T, q[element].T, 3);

    }
    return n;
}

ZcmDisparityMap* ZcmDisparityMap_copy(const ZcmDisparityMap* p)
{
    ZcmDisparityMap* q = (ZcmDisparityMap*) malloc(sizeof(ZcmDisparityMap));
    __ZcmDisparityMap_clone_array(p, q, 1);
    return q;
}

void ZcmDisparityMap_destroy(ZcmDisparityMap* p)
{
    __ZcmDisparityMap_decode_array_cleanup(p, 1);
    free(p);
}

int ZcmDisparityMap_publish(zcm_t* zcm, const char* channel, const ZcmDisparityMap* p)
{
      uint32_t max_data_size = ZcmDisparityMap_encoded_size (p);
      uint8_t* buf = (uint8_t*) malloc (max_data_size);
      if (!buf) return -1;
      int data_size = ZcmDisparityMap_encode (buf, 0, max_data_size, p);
      if (data_size < 0) {
          free (buf);
          return data_size;
      }
      int status = zcm_publish (zcm, channel, buf, (uint32_t)data_size);
      free (buf);
      return status;
}

struct _ZcmDisparityMap_subscription_t {
    ZcmDisparityMap_handler_t user_handler;
    void* userdata;
    zcm_sub_t* z_sub;
};
static
void ZcmDisparityMap_handler_stub (const zcm_recv_buf_t* rbuf,
                            const char* channel, void* userdata)
{
    int status;
    ZcmDisparityMap p;
    memset(&p, 0, sizeof(ZcmDisparityMap));
    status = ZcmDisparityMap_decode (rbuf->data, 0, rbuf->data_size, &p);
    if (status < 0) {
        fprintf (stderr, "error %d decoding ZcmDisparityMap!!!\n", status);
        return;
    }

    ZcmDisparityMap_subscription_t* h = (ZcmDisparityMap_subscription_t*) userdata;
    h->user_handler (rbuf, channel, &p, h->userdata);

    ZcmDisparityMap_decode_cleanup (&p);
}

ZcmDisparityMap_subscription_t* ZcmDisparityMap_subscribe (zcm_t* zcm,
                    const char* channel,
                    ZcmDisparityMap_handler_t f, void* userdata)
{
    ZcmDisparityMap_subscription_t* n = (ZcmDisparityMap_subscription_t*)
                       malloc(sizeof(ZcmDisparityMap_subscription_t));
    n->user_handler = f;
    n->userdata = userdata;
    n->z_sub = zcm_subscribe (zcm, channel,
                              ZcmDisparityMap_handler_stub, n);
    if (n->z_sub == NULL) {
        fprintf (stderr,"couldn't reg ZcmDisparityMap ZCM handler!\n");
        free (n);
        return NULL;
    }
    return n;
}

int ZcmDisparityMap_unsubscribe(zcm_t* zcm, ZcmDisparityMap_subscription_t* hid)
{
    int status = zcm_unsubscribe (zcm, hid->z_sub);
    if (0 != status) {
        fprintf(stderr,
           "couldn't unsubscribe ZcmDisparityMap_handler %p!\n", hid);
        return -1;
    }
    free (hid);
    return 0;
}

