"""ZCM type definitions
This file automatically generated by zcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

from ZcmStereoPairPoint3D import ZcmStereoPairPoint3D

from ZcmService import ZcmService

class ZcmDisparityPoints(object):
    __slots__ = ["service", "count", "points"]

    def __init__(self):
        self.service = ZcmService()
        self.count = 0
        self.points = []

    def encode(self):
        buf = BytesIO()
        buf.write(ZcmDisparityPoints._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        assert self.service._get_packed_fingerprint() == ZcmService._get_packed_fingerprint()
        self.service._encode_one(buf)
        buf.write(struct.pack(">i", self.count))
        for i0 in range(self.count):
            assert self.points[i0]._get_packed_fingerprint() == ZcmStereoPairPoint3D._get_packed_fingerprint()
            self.points[i0]._encode_one(buf)

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != ZcmDisparityPoints._get_packed_fingerprint():
            raise ValueError("Decode error")
        return ZcmDisparityPoints._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = ZcmDisparityPoints()
        self.service = ZcmService._decode_one(buf)
        self.count = struct.unpack(">i", buf.read(4))[0]
        self.points = []
        for i0 in range(self.count):
            self.points.append(ZcmStereoPairPoint3D._decode_one(buf))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if ZcmDisparityPoints in parents: return 0
        newparents = parents + [ZcmDisparityPoints]
        tmphash = (0x2988e1e5c7c2505+ ZcmService._get_hash_recursive(newparents)+ ZcmStereoPairPoint3D._get_hash_recursive(newparents)) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + ((tmphash>>63)&0x1)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if ZcmDisparityPoints._packed_fingerprint is None:
            ZcmDisparityPoints._packed_fingerprint = struct.pack(">Q", ZcmDisparityPoints._get_hash_recursive([]))
        return ZcmDisparityPoints._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

