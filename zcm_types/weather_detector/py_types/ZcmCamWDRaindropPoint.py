"""ZCM type definitions
This file automatically generated by zcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class ZcmCamWDRaindropPoint(object):
    __slots__ = ["x", "y", "radius", "area", "blur_rate"]

    RAINDROP_BLUR_RATE_UNCHANGED = 0
    RAINDROP_BLUR_RATE_LOW = 1
    RAINDROP_BLUR_RATE_MIDDLE = 2
    RAINDROP_BLUR_RATE_HIGH = 3
    RAINDROP_BLUR_RATE_VHIGH = 4

    def __init__(self):
        self.x = 0
        self.y = 0
        self.radius = 0
        self.area = 0
        self.blur_rate = 0

    def encode(self):
        buf = BytesIO()
        buf.write(ZcmCamWDRaindropPoint._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">iiiib", self.x, self.y, self.radius, self.area, self.blur_rate))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != ZcmCamWDRaindropPoint._get_packed_fingerprint():
            raise ValueError("Decode error")
        return ZcmCamWDRaindropPoint._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = ZcmCamWDRaindropPoint()
        self.x, self.y, self.radius, self.area, self.blur_rate = struct.unpack(">iiiib", buf.read(17))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if ZcmCamWDRaindropPoint in parents: return 0
        tmphash = (0xa2b19692eaf05a2d) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + ((tmphash>>63)&0x1)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if ZcmCamWDRaindropPoint._packed_fingerprint is None:
            ZcmCamWDRaindropPoint._packed_fingerprint = struct.pack(">Q", ZcmCamWDRaindropPoint._get_hash_recursive([]))
        return ZcmCamWDRaindropPoint._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

