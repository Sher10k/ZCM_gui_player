/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by zcm-gen
 **/

#include <zcm/zcm_coretypes.h>

#ifndef __ZcmCamWDRaindropPoint_hpp__
#define __ZcmCamWDRaindropPoint_hpp__



/**
 * -------------------------------------------------------------------------------------
 * @struct ZcmCamWDRaindropPoint
 * @brief Информация о координате точки (пикселл на изображении), 
 * являющейся центром капли на изображении, а также о 
 * радиусе, площади и степени размытия капли.
 * @var ZcmCamWDRaindropPoint::x
 * @brief - координата (пикселл) центра капли по оси X изображения. 
 * Принимает значения от 0 до image::width - 1 (входного изображения).
 * @var ZcmCamWDRaindropPoint::y
 * @brief - координата (пикселл) центра капли по оси Y изображения. 
 * Принимает значения от 0 до image::height - 1 (входного изображения).
 * @var ZcmCamWDRaindropPoint::radius
 * @brief - радиус капли на переднем плане камеры. 
 * Принимает значения от 0 до image::height / 2 или 
 * от 0 до image::width / 2 (входного изображения).
 * @var ZcmCamWDRaindropPoint::area
 * @brief - площадь капли на камере. 
 * Принимает значения от 0 до image::total() (входного изображения).
 * @var ZcmCamWDRaindropPoint::blur_rate
 * @brief - Информация о степени размытия: \n
 *          RAINDROP_BLUR_RATE_UNCHANGED - значение по умолчанию,
 *                                         если капли отсутствуют \n
 *          RAINDROP_BLUR_RATE_LOW - слабое размытие \n
 *          RAINDROP_BLUR_RATE_MIDDLE - средней размытости \n
 *          RAINDROP_BLUR_RATE_HIGH - сильно размыто \n
 *          RAINDROP_BLUR_RATE_VHIGH - очень сильно рназмыто \n
 *
 */
class ZcmCamWDRaindropPoint
{
    public:
        int32_t    x;

        int32_t    y;

        int32_t    radius;

        int32_t    area;

        int8_t     blur_rate;

    public:
        #if __cplusplus > 199711L /* if c++11 */
        static constexpr int8_t   RAINDROP_BLUR_RATE_UNCHANGED = 0;
        static constexpr int8_t   RAINDROP_BLUR_RATE_LOW = 1;
        static constexpr int8_t   RAINDROP_BLUR_RATE_MIDDLE = 2;
        static constexpr int8_t   RAINDROP_BLUR_RATE_HIGH = 3;
        static constexpr int8_t   RAINDROP_BLUR_RATE_VHIGH = 4;
        #else
        static const     int8_t   RAINDROP_BLUR_RATE_UNCHANGED = 0;
        static const     int8_t   RAINDROP_BLUR_RATE_LOW = 1;
        static const     int8_t   RAINDROP_BLUR_RATE_MIDDLE = 2;
        static const     int8_t   RAINDROP_BLUR_RATE_HIGH = 3;
        static const     int8_t   RAINDROP_BLUR_RATE_VHIGH = 4;
        #endif

    public:
        /**
         * Destructs a message properly if anything inherits from it
        */
        virtual ~ZcmCamWDRaindropPoint() {}

        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void* buf, uint32_t offset, uint32_t maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline uint32_t getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to reqad while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void* buf, uint32_t offset, uint32_t maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "ZcmCamWDRaindropPoint"
         */
        inline static const char* getTypeName();

        // ZCM support functions. Users should not call these
        inline int      _encodeNoHash(void* buf, uint32_t offset, uint32_t maxlen) const;
        inline uint32_t _getEncodedSizeNoHash() const;
        inline int      _decodeNoHash(const void* buf, uint32_t offset, uint32_t maxlen);
        inline static uint64_t _computeHash(const __zcm_hash_ptr* p);
};

int ZcmCamWDRaindropPoint::encode(void* buf, uint32_t offset, uint32_t maxlen) const
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = (int64_t)getHash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int ZcmCamWDRaindropPoint::decode(const void* buf, uint32_t offset, uint32_t maxlen)
{
    uint32_t pos = 0;
    int thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

uint32_t ZcmCamWDRaindropPoint::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t ZcmCamWDRaindropPoint::getHash()
{
    static int64_t hash = _computeHash(NULL);
    return hash;
}

const char* ZcmCamWDRaindropPoint::getTypeName()
{
    return "ZcmCamWDRaindropPoint";
}

int ZcmCamWDRaindropPoint::_encodeNoHash(void* buf, uint32_t offset, uint32_t maxlen) const
{
    uint32_t pos = 0;
    int thislen;

    thislen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->x, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->y, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->radius, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->area, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->blur_rate, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int ZcmCamWDRaindropPoint::_decodeNoHash(const void* buf, uint32_t offset, uint32_t maxlen)
{
    uint32_t pos = 0;
    int thislen;

    thislen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->x, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->y, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->radius, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->area, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->blur_rate, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    return pos;
}

uint32_t ZcmCamWDRaindropPoint::_getEncodedSizeNoHash() const
{
    uint32_t enc_size = 0;
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    return enc_size;
}

uint64_t ZcmCamWDRaindropPoint::_computeHash(const __zcm_hash_ptr*)
{
    uint64_t hash = (uint64_t)0xa2b19692eaf05a2dLL;
    return (hash<<1) + ((hash>>63)&1);
}

#endif
