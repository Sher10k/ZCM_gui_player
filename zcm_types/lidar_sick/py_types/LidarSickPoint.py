"""ZCM type definitions
This file automatically generated by zcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class LidarSickPoint(object):
    __slots__ = ["echo_id", "layer_id", "point_type", "horizontal_angle", "radial_distance", "x", "y", "z", "echo_pulse_width"]

    def __init__(self):
        self.echo_id = 0
        self.layer_id = 0
        self.point_type = 0
        self.horizontal_angle = 0
        self.radial_distance = 0.0
        self.x = 0.0
        self.y = 0.0
        self.z = 0.0
        self.echo_pulse_width = 0.0

    def encode(self):
        buf = BytesIO()
        buf.write(LidarSickPoint._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">hhiiddddd", self.echo_id, self.layer_id, self.point_type, self.horizontal_angle, self.radial_distance, self.x, self.y, self.z, self.echo_pulse_width))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != LidarSickPoint._get_packed_fingerprint():
            raise ValueError("Decode error")
        return LidarSickPoint._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = LidarSickPoint()
        self.echo_id, self.layer_id, self.point_type, self.horizontal_angle, self.radial_distance, self.x, self.y, self.z, self.echo_pulse_width = struct.unpack(">hhiiddddd", buf.read(52))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if LidarSickPoint in parents: return 0
        tmphash = (0x893f0c21253c1834) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + ((tmphash>>63)&0x1)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if LidarSickPoint._packed_fingerprint is None:
            LidarSickPoint._packed_fingerprint = struct.pack(">Q", LidarSickPoint._get_hash_recursive([]))
        return LidarSickPoint._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

