// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by zcm-gen

#include <string.h>
#include "LidarSickWarnings.h"

static int __LidarSickWarnings_hash_computed;
static uint64_t __LidarSickWarnings_hash;

uint64_t __LidarSickWarnings_hash_recursive(const __zcm_hash_ptr* p)
{
    const __zcm_hash_ptr* fp;
    for (fp = p; fp != NULL; fp = fp->parent)
        if (fp->v == __LidarSickWarnings_get_hash)
            return 0;

    __zcm_hash_ptr cp;
    cp.parent =  p;
    cp.v = (void*)__LidarSickWarnings_get_hash;
    (void) cp;

    uint64_t hash = (uint64_t)0x2370de4e6af21b93LL
         + __ZcmService_hash_recursive(&cp)
         + __boolean_hash_recursive(&cp)
         + __boolean_hash_recursive(&cp)
         + __boolean_hash_recursive(&cp)
         + __boolean_hash_recursive(&cp)
         + __boolean_hash_recursive(&cp)
         + __boolean_hash_recursive(&cp)
         + __boolean_hash_recursive(&cp)
         + __boolean_hash_recursive(&cp)
         + __boolean_hash_recursive(&cp)
         + __boolean_hash_recursive(&cp)
         + __boolean_hash_recursive(&cp)
         + __boolean_hash_recursive(&cp)
         + __boolean_hash_recursive(&cp)
         + __boolean_hash_recursive(&cp)
         + __boolean_hash_recursive(&cp)
         + __boolean_hash_recursive(&cp)
         + __boolean_hash_recursive(&cp)
         + __boolean_hash_recursive(&cp)
         + __boolean_hash_recursive(&cp)
         + __boolean_hash_recursive(&cp)
        ;

    return (hash<<1) + ((hash>>63)&1);
}

int64_t __LidarSickWarnings_get_hash(void)
{
    if (!__LidarSickWarnings_hash_computed) {
        __LidarSickWarnings_hash = (int64_t)__LidarSickWarnings_hash_recursive(NULL);
        __LidarSickWarnings_hash_computed = 1;
    }

    return __LidarSickWarnings_hash;
}

int __LidarSickWarnings_encode_array(void* buf, uint32_t offset, uint32_t maxlen, const LidarSickWarnings* p, uint32_t elements)
{
    uint32_t pos = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        thislen = __ZcmService_encode_array(buf, offset + pos, maxlen - pos, &(p[element].service), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &(p[element].contact_support), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &(p[element].low_temperature), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &(p[element].high_temperature), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &(p[element].check_sync_scan_freq), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &(p[element].startpulse_not_detected_1), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &(p[element].startpulse_not_detected_2), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &(p[element].can_blocked), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &(p[element].eth_blocked), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &(p[element].check_eth_data), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &(p[element].incorrect_command_received), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &(p[element].memory_access_failure), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &(p[element].segment_overflow), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &(p[element].ego_motion), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &(p[element].mounting_pos), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &(p[element].calc_freq), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &(p[element].no_ntp_time), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &(p[element].no_time_sync_pps), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &(p[element].no_time_sync_command), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &(p[element].no_time_sync), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &(p[element].slight_deviation), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int LidarSickWarnings_encode(void* buf, uint32_t offset, uint32_t maxlen, const LidarSickWarnings* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __LidarSickWarnings_get_hash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    thislen = __LidarSickWarnings_encode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

uint32_t __LidarSickWarnings_encoded_array_size(const LidarSickWarnings* p, uint32_t elements)
{
    uint32_t size = 0, element;
    for (element = 0; element < elements; ++element) {

        size += __ZcmService_encoded_array_size(&(p[element].service), 1);

        size += __boolean_encoded_array_size(&(p[element].contact_support), 1);

        size += __boolean_encoded_array_size(&(p[element].low_temperature), 1);

        size += __boolean_encoded_array_size(&(p[element].high_temperature), 1);

        size += __boolean_encoded_array_size(&(p[element].check_sync_scan_freq), 1);

        size += __boolean_encoded_array_size(&(p[element].startpulse_not_detected_1), 1);

        size += __boolean_encoded_array_size(&(p[element].startpulse_not_detected_2), 1);

        size += __boolean_encoded_array_size(&(p[element].can_blocked), 1);

        size += __boolean_encoded_array_size(&(p[element].eth_blocked), 1);

        size += __boolean_encoded_array_size(&(p[element].check_eth_data), 1);

        size += __boolean_encoded_array_size(&(p[element].incorrect_command_received), 1);

        size += __boolean_encoded_array_size(&(p[element].memory_access_failure), 1);

        size += __boolean_encoded_array_size(&(p[element].segment_overflow), 1);

        size += __boolean_encoded_array_size(&(p[element].ego_motion), 1);

        size += __boolean_encoded_array_size(&(p[element].mounting_pos), 1);

        size += __boolean_encoded_array_size(&(p[element].calc_freq), 1);

        size += __boolean_encoded_array_size(&(p[element].no_ntp_time), 1);

        size += __boolean_encoded_array_size(&(p[element].no_time_sync_pps), 1);

        size += __boolean_encoded_array_size(&(p[element].no_time_sync_command), 1);

        size += __boolean_encoded_array_size(&(p[element].no_time_sync), 1);

        size += __boolean_encoded_array_size(&(p[element].slight_deviation), 1);

    }
    return size;
}

uint32_t LidarSickWarnings_encoded_size(const LidarSickWarnings* p)
{
    return 8 + __LidarSickWarnings_encoded_array_size(p, 1);
}

uint32_t LidarSickWarnings_struct_size(void)
{
    return sizeof(LidarSickWarnings);
}

uint32_t LidarSickWarnings_num_fields(void)
{
    return 21;
}

int LidarSickWarnings_get_field(const LidarSickWarnings* p, uint32_t i, zcm_field_t* f)
{
    if (i >= LidarSickWarnings_num_fields())
        return 1;
    
    switch (i) {
    
        case 0: {
            /* ZcmService */
            f->name = "service";
            f->type = ZCM_FIELD_USER_TYPE;
            f->typestr = "ZcmService";
            f->num_dim = 0;
            f->data = (void*) &p->service;
            return 0;
        }
        
        case 1: {
            f->name = "contact_support";
            f->type = ZCM_FIELD_BOOLEAN;
            f->typestr = "boolean";
            f->num_dim = 0;
            f->data = (void*) &p->contact_support;
            return 0;
        }
        
        case 2: {
            f->name = "low_temperature";
            f->type = ZCM_FIELD_BOOLEAN;
            f->typestr = "boolean";
            f->num_dim = 0;
            f->data = (void*) &p->low_temperature;
            return 0;
        }
        
        case 3: {
            f->name = "high_temperature";
            f->type = ZCM_FIELD_BOOLEAN;
            f->typestr = "boolean";
            f->num_dim = 0;
            f->data = (void*) &p->high_temperature;
            return 0;
        }
        
        case 4: {
            f->name = "check_sync_scan_freq";
            f->type = ZCM_FIELD_BOOLEAN;
            f->typestr = "boolean";
            f->num_dim = 0;
            f->data = (void*) &p->check_sync_scan_freq;
            return 0;
        }
        
        case 5: {
            f->name = "startpulse_not_detected_1";
            f->type = ZCM_FIELD_BOOLEAN;
            f->typestr = "boolean";
            f->num_dim = 0;
            f->data = (void*) &p->startpulse_not_detected_1;
            return 0;
        }
        
        case 6: {
            f->name = "startpulse_not_detected_2";
            f->type = ZCM_FIELD_BOOLEAN;
            f->typestr = "boolean";
            f->num_dim = 0;
            f->data = (void*) &p->startpulse_not_detected_2;
            return 0;
        }
        
        case 7: {
            f->name = "can_blocked";
            f->type = ZCM_FIELD_BOOLEAN;
            f->typestr = "boolean";
            f->num_dim = 0;
            f->data = (void*) &p->can_blocked;
            return 0;
        }
        
        case 8: {
            f->name = "eth_blocked";
            f->type = ZCM_FIELD_BOOLEAN;
            f->typestr = "boolean";
            f->num_dim = 0;
            f->data = (void*) &p->eth_blocked;
            return 0;
        }
        
        case 9: {
            f->name = "check_eth_data";
            f->type = ZCM_FIELD_BOOLEAN;
            f->typestr = "boolean";
            f->num_dim = 0;
            f->data = (void*) &p->check_eth_data;
            return 0;
        }
        
        case 10: {
            f->name = "incorrect_command_received";
            f->type = ZCM_FIELD_BOOLEAN;
            f->typestr = "boolean";
            f->num_dim = 0;
            f->data = (void*) &p->incorrect_command_received;
            return 0;
        }
        
        case 11: {
            f->name = "memory_access_failure";
            f->type = ZCM_FIELD_BOOLEAN;
            f->typestr = "boolean";
            f->num_dim = 0;
            f->data = (void*) &p->memory_access_failure;
            return 0;
        }
        
        case 12: {
            f->name = "segment_overflow";
            f->type = ZCM_FIELD_BOOLEAN;
            f->typestr = "boolean";
            f->num_dim = 0;
            f->data = (void*) &p->segment_overflow;
            return 0;
        }
        
        case 13: {
            f->name = "ego_motion";
            f->type = ZCM_FIELD_BOOLEAN;
            f->typestr = "boolean";
            f->num_dim = 0;
            f->data = (void*) &p->ego_motion;
            return 0;
        }
        
        case 14: {
            f->name = "mounting_pos";
            f->type = ZCM_FIELD_BOOLEAN;
            f->typestr = "boolean";
            f->num_dim = 0;
            f->data = (void*) &p->mounting_pos;
            return 0;
        }
        
        case 15: {
            f->name = "calc_freq";
            f->type = ZCM_FIELD_BOOLEAN;
            f->typestr = "boolean";
            f->num_dim = 0;
            f->data = (void*) &p->calc_freq;
            return 0;
        }
        
        case 16: {
            f->name = "no_ntp_time";
            f->type = ZCM_FIELD_BOOLEAN;
            f->typestr = "boolean";
            f->num_dim = 0;
            f->data = (void*) &p->no_ntp_time;
            return 0;
        }
        
        case 17: {
            f->name = "no_time_sync_pps";
            f->type = ZCM_FIELD_BOOLEAN;
            f->typestr = "boolean";
            f->num_dim = 0;
            f->data = (void*) &p->no_time_sync_pps;
            return 0;
        }
        
        case 18: {
            f->name = "no_time_sync_command";
            f->type = ZCM_FIELD_BOOLEAN;
            f->typestr = "boolean";
            f->num_dim = 0;
            f->data = (void*) &p->no_time_sync_command;
            return 0;
        }
        
        case 19: {
            f->name = "no_time_sync";
            f->type = ZCM_FIELD_BOOLEAN;
            f->typestr = "boolean";
            f->num_dim = 0;
            f->data = (void*) &p->no_time_sync;
            return 0;
        }
        
        case 20: {
            f->name = "slight_deviation";
            f->type = ZCM_FIELD_BOOLEAN;
            f->typestr = "boolean";
            f->num_dim = 0;
            f->data = (void*) &p->slight_deviation;
            return 0;
        }
        
        default:
            return 1;
    }
}

const zcm_type_info_t* LidarSickWarnings_get_type_info(void)
{
    static int init = 0;
    static zcm_type_info_t typeinfo;
    if (!init) {
        typeinfo.encode         = (zcm_encode_t) LidarSickWarnings_encode;
        typeinfo.decode         = (zcm_decode_t) LidarSickWarnings_decode;
        typeinfo.decode_cleanup = (zcm_decode_cleanup_t) LidarSickWarnings_decode_cleanup;
        typeinfo.encoded_size   = (zcm_encoded_size_t) LidarSickWarnings_encoded_size;
        typeinfo.struct_size    = (zcm_struct_size_t)  LidarSickWarnings_struct_size;
        typeinfo.num_fields     = (zcm_num_fields_t) LidarSickWarnings_num_fields;
        typeinfo.get_field      = (zcm_get_field_t) LidarSickWarnings_get_field;
        typeinfo.get_hash       = (zcm_get_hash_t) __LidarSickWarnings_get_hash;
    }
    
    return &typeinfo;
}
int __LidarSickWarnings_decode_array(const void* buf, uint32_t offset, uint32_t maxlen, LidarSickWarnings* p, uint32_t elements)
{
    uint32_t pos = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        thislen = __ZcmService_decode_array(buf, offset + pos, maxlen - pos, &(p[element].service), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &(p[element].contact_support), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &(p[element].low_temperature), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &(p[element].high_temperature), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &(p[element].check_sync_scan_freq), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &(p[element].startpulse_not_detected_1), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &(p[element].startpulse_not_detected_2), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &(p[element].can_blocked), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &(p[element].eth_blocked), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &(p[element].check_eth_data), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &(p[element].incorrect_command_received), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &(p[element].memory_access_failure), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &(p[element].segment_overflow), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &(p[element].ego_motion), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &(p[element].mounting_pos), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &(p[element].calc_freq), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &(p[element].no_ntp_time), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &(p[element].no_time_sync_pps), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &(p[element].no_time_sync_command), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &(p[element].no_time_sync), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &(p[element].slight_deviation), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int __LidarSickWarnings_decode_array_cleanup(LidarSickWarnings* p, uint32_t elements)
{
    uint32_t element;
    for (element = 0; element < elements; ++element) {

        __ZcmService_decode_array_cleanup(&(p[element].service), 1);

        __boolean_decode_array_cleanup(&(p[element].contact_support), 1);

        __boolean_decode_array_cleanup(&(p[element].low_temperature), 1);

        __boolean_decode_array_cleanup(&(p[element].high_temperature), 1);

        __boolean_decode_array_cleanup(&(p[element].check_sync_scan_freq), 1);

        __boolean_decode_array_cleanup(&(p[element].startpulse_not_detected_1), 1);

        __boolean_decode_array_cleanup(&(p[element].startpulse_not_detected_2), 1);

        __boolean_decode_array_cleanup(&(p[element].can_blocked), 1);

        __boolean_decode_array_cleanup(&(p[element].eth_blocked), 1);

        __boolean_decode_array_cleanup(&(p[element].check_eth_data), 1);

        __boolean_decode_array_cleanup(&(p[element].incorrect_command_received), 1);

        __boolean_decode_array_cleanup(&(p[element].memory_access_failure), 1);

        __boolean_decode_array_cleanup(&(p[element].segment_overflow), 1);

        __boolean_decode_array_cleanup(&(p[element].ego_motion), 1);

        __boolean_decode_array_cleanup(&(p[element].mounting_pos), 1);

        __boolean_decode_array_cleanup(&(p[element].calc_freq), 1);

        __boolean_decode_array_cleanup(&(p[element].no_ntp_time), 1);

        __boolean_decode_array_cleanup(&(p[element].no_time_sync_pps), 1);

        __boolean_decode_array_cleanup(&(p[element].no_time_sync_command), 1);

        __boolean_decode_array_cleanup(&(p[element].no_time_sync), 1);

        __boolean_decode_array_cleanup(&(p[element].slight_deviation), 1);

    }
    return 0;
}

int LidarSickWarnings_decode(const void* buf, uint32_t offset, uint32_t maxlen, LidarSickWarnings* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __LidarSickWarnings_get_hash();

    int64_t this_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (this_hash != hash) return -1;

    thislen = __LidarSickWarnings_decode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int LidarSickWarnings_decode_cleanup(LidarSickWarnings* p)
{
    return __LidarSickWarnings_decode_array_cleanup(p, 1);
}

uint32_t __LidarSickWarnings_clone_array(const LidarSickWarnings* p, LidarSickWarnings* q, uint32_t elements)
{
    uint32_t n = 0, element;
    for (element = 0; element < elements; ++element) {

        n += __ZcmService_clone_array(&(p[element].service), &(q[element].service), 1);

        n += __boolean_clone_array(&(p[element].contact_support), &(q[element].contact_support), 1);

        n += __boolean_clone_array(&(p[element].low_temperature), &(q[element].low_temperature), 1);

        n += __boolean_clone_array(&(p[element].high_temperature), &(q[element].high_temperature), 1);

        n += __boolean_clone_array(&(p[element].check_sync_scan_freq), &(q[element].check_sync_scan_freq), 1);

        n += __boolean_clone_array(&(p[element].startpulse_not_detected_1), &(q[element].startpulse_not_detected_1), 1);

        n += __boolean_clone_array(&(p[element].startpulse_not_detected_2), &(q[element].startpulse_not_detected_2), 1);

        n += __boolean_clone_array(&(p[element].can_blocked), &(q[element].can_blocked), 1);

        n += __boolean_clone_array(&(p[element].eth_blocked), &(q[element].eth_blocked), 1);

        n += __boolean_clone_array(&(p[element].check_eth_data), &(q[element].check_eth_data), 1);

        n += __boolean_clone_array(&(p[element].incorrect_command_received), &(q[element].incorrect_command_received), 1);

        n += __boolean_clone_array(&(p[element].memory_access_failure), &(q[element].memory_access_failure), 1);

        n += __boolean_clone_array(&(p[element].segment_overflow), &(q[element].segment_overflow), 1);

        n += __boolean_clone_array(&(p[element].ego_motion), &(q[element].ego_motion), 1);

        n += __boolean_clone_array(&(p[element].mounting_pos), &(q[element].mounting_pos), 1);

        n += __boolean_clone_array(&(p[element].calc_freq), &(q[element].calc_freq), 1);

        n += __boolean_clone_array(&(p[element].no_ntp_time), &(q[element].no_ntp_time), 1);

        n += __boolean_clone_array(&(p[element].no_time_sync_pps), &(q[element].no_time_sync_pps), 1);

        n += __boolean_clone_array(&(p[element].no_time_sync_command), &(q[element].no_time_sync_command), 1);

        n += __boolean_clone_array(&(p[element].no_time_sync), &(q[element].no_time_sync), 1);

        n += __boolean_clone_array(&(p[element].slight_deviation), &(q[element].slight_deviation), 1);

    }
    return n;
}

LidarSickWarnings* LidarSickWarnings_copy(const LidarSickWarnings* p)
{
    LidarSickWarnings* q = (LidarSickWarnings*) malloc(sizeof(LidarSickWarnings));
    __LidarSickWarnings_clone_array(p, q, 1);
    return q;
}

void LidarSickWarnings_destroy(LidarSickWarnings* p)
{
    __LidarSickWarnings_decode_array_cleanup(p, 1);
    free(p);
}

int LidarSickWarnings_publish(zcm_t* zcm, const char* channel, const LidarSickWarnings* p)
{
      uint32_t max_data_size = LidarSickWarnings_encoded_size (p);
      uint8_t* buf = (uint8_t*) malloc (max_data_size);
      if (!buf) return -1;
      int data_size = LidarSickWarnings_encode (buf, 0, max_data_size, p);
      if (data_size < 0) {
          free (buf);
          return data_size;
      }
      int status = zcm_publish (zcm, channel, buf, (uint32_t)data_size);
      free (buf);
      return status;
}

struct _LidarSickWarnings_subscription_t {
    LidarSickWarnings_handler_t user_handler;
    void* userdata;
    zcm_sub_t* z_sub;
};
static
void LidarSickWarnings_handler_stub (const zcm_recv_buf_t* rbuf,
                            const char* channel, void* userdata)
{
    int status;
    LidarSickWarnings p;
    memset(&p, 0, sizeof(LidarSickWarnings));
    status = LidarSickWarnings_decode (rbuf->data, 0, rbuf->data_size, &p);
    if (status < 0) {
        fprintf (stderr, "error %d decoding LidarSickWarnings!!!\n", status);
        return;
    }

    LidarSickWarnings_subscription_t* h = (LidarSickWarnings_subscription_t*) userdata;
    h->user_handler (rbuf, channel, &p, h->userdata);

    LidarSickWarnings_decode_cleanup (&p);
}

LidarSickWarnings_subscription_t* LidarSickWarnings_subscribe (zcm_t* zcm,
                    const char* channel,
                    LidarSickWarnings_handler_t f, void* userdata)
{
    LidarSickWarnings_subscription_t* n = (LidarSickWarnings_subscription_t*)
                       malloc(sizeof(LidarSickWarnings_subscription_t));
    n->user_handler = f;
    n->userdata = userdata;
    n->z_sub = zcm_subscribe (zcm, channel,
                              LidarSickWarnings_handler_stub, n);
    if (n->z_sub == NULL) {
        fprintf (stderr,"couldn't reg LidarSickWarnings ZCM handler!\n");
        free (n);
        return NULL;
    }
    return n;
}

int LidarSickWarnings_unsubscribe(zcm_t* zcm, LidarSickWarnings_subscription_t* hid)
{
    int status = zcm_unsubscribe (zcm, hid->z_sub);
    if (0 != status) {
        fprintf(stderr,
           "couldn't unsubscribe LidarSickWarnings_handler %p!\n", hid);
        return -1;
    }
    free (hid);
    return 0;
}

