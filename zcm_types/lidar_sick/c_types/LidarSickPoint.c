// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by zcm-gen

#include <string.h>
#include "LidarSickPoint.h"

static int __LidarSickPoint_hash_computed;
static uint64_t __LidarSickPoint_hash;

uint64_t __LidarSickPoint_hash_recursive(const __zcm_hash_ptr* p)
{
    const __zcm_hash_ptr* fp;
    for (fp = p; fp != NULL; fp = fp->parent)
        if (fp->v == __LidarSickPoint_get_hash)
            return 0;

    __zcm_hash_ptr cp;
    cp.parent =  p;
    cp.v = (void*)__LidarSickPoint_get_hash;
    (void) cp;

    uint64_t hash = (uint64_t)0x893f0c21253c1834LL
         + __int16_t_hash_recursive(&cp)
         + __int16_t_hash_recursive(&cp)
         + __int32_t_hash_recursive(&cp)
         + __int32_t_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
        ;

    return (hash<<1) + ((hash>>63)&1);
}

int64_t __LidarSickPoint_get_hash(void)
{
    if (!__LidarSickPoint_hash_computed) {
        __LidarSickPoint_hash = (int64_t)__LidarSickPoint_hash_recursive(NULL);
        __LidarSickPoint_hash_computed = 1;
    }

    return __LidarSickPoint_hash;
}

int __LidarSickPoint_encode_array(void* buf, uint32_t offset, uint32_t maxlen, const LidarSickPoint* p, uint32_t elements)
{
    uint32_t pos = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        thislen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].echo_id), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].layer_id), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].point_type), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].horizontal_angle), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].radial_distance), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].x), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].y), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].z), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].echo_pulse_width), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int LidarSickPoint_encode(void* buf, uint32_t offset, uint32_t maxlen, const LidarSickPoint* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __LidarSickPoint_get_hash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    thislen = __LidarSickPoint_encode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

uint32_t __LidarSickPoint_encoded_array_size(const LidarSickPoint* p, uint32_t elements)
{
    uint32_t size = 0, element;
    for (element = 0; element < elements; ++element) {

        size += __int16_t_encoded_array_size(&(p[element].echo_id), 1);

        size += __int16_t_encoded_array_size(&(p[element].layer_id), 1);

        size += __int32_t_encoded_array_size(&(p[element].point_type), 1);

        size += __int32_t_encoded_array_size(&(p[element].horizontal_angle), 1);

        size += __double_encoded_array_size(&(p[element].radial_distance), 1);

        size += __double_encoded_array_size(&(p[element].x), 1);

        size += __double_encoded_array_size(&(p[element].y), 1);

        size += __double_encoded_array_size(&(p[element].z), 1);

        size += __double_encoded_array_size(&(p[element].echo_pulse_width), 1);

    }
    return size;
}

uint32_t LidarSickPoint_encoded_size(const LidarSickPoint* p)
{
    return 8 + __LidarSickPoint_encoded_array_size(p, 1);
}

uint32_t LidarSickPoint_struct_size(void)
{
    return sizeof(LidarSickPoint);
}

uint32_t LidarSickPoint_num_fields(void)
{
    return 9;
}

int LidarSickPoint_get_field(const LidarSickPoint* p, uint32_t i, zcm_field_t* f)
{
    if (i >= LidarSickPoint_num_fields())
        return 1;
    
    switch (i) {
    
        case 0: {
            f->name = "echo_id";
            f->type = ZCM_FIELD_INT16_T;
            f->typestr = "int16_t";
            f->num_dim = 0;
            f->data = (void*) &p->echo_id;
            return 0;
        }
        
        case 1: {
            f->name = "layer_id";
            f->type = ZCM_FIELD_INT16_T;
            f->typestr = "int16_t";
            f->num_dim = 0;
            f->data = (void*) &p->layer_id;
            return 0;
        }
        
        case 2: {
            f->name = "point_type";
            f->type = ZCM_FIELD_INT32_T;
            f->typestr = "int32_t";
            f->num_dim = 0;
            f->data = (void*) &p->point_type;
            return 0;
        }
        
        case 3: {
            f->name = "horizontal_angle";
            f->type = ZCM_FIELD_INT32_T;
            f->typestr = "int32_t";
            f->num_dim = 0;
            f->data = (void*) &p->horizontal_angle;
            return 0;
        }
        
        case 4: {
            f->name = "radial_distance";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 0;
            f->data = (void*) &p->radial_distance;
            return 0;
        }
        
        case 5: {
            f->name = "x";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 0;
            f->data = (void*) &p->x;
            return 0;
        }
        
        case 6: {
            f->name = "y";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 0;
            f->data = (void*) &p->y;
            return 0;
        }
        
        case 7: {
            f->name = "z";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 0;
            f->data = (void*) &p->z;
            return 0;
        }
        
        case 8: {
            f->name = "echo_pulse_width";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 0;
            f->data = (void*) &p->echo_pulse_width;
            return 0;
        }
        
        default:
            return 1;
    }
}

const zcm_type_info_t* LidarSickPoint_get_type_info(void)
{
    static int init = 0;
    static zcm_type_info_t typeinfo;
    if (!init) {
        typeinfo.encode         = (zcm_encode_t) LidarSickPoint_encode;
        typeinfo.decode         = (zcm_decode_t) LidarSickPoint_decode;
        typeinfo.decode_cleanup = (zcm_decode_cleanup_t) LidarSickPoint_decode_cleanup;
        typeinfo.encoded_size   = (zcm_encoded_size_t) LidarSickPoint_encoded_size;
        typeinfo.struct_size    = (zcm_struct_size_t)  LidarSickPoint_struct_size;
        typeinfo.num_fields     = (zcm_num_fields_t) LidarSickPoint_num_fields;
        typeinfo.get_field      = (zcm_get_field_t) LidarSickPoint_get_field;
        typeinfo.get_hash       = (zcm_get_hash_t) __LidarSickPoint_get_hash;
    }
    
    return &typeinfo;
}
int __LidarSickPoint_decode_array(const void* buf, uint32_t offset, uint32_t maxlen, LidarSickPoint* p, uint32_t elements)
{
    uint32_t pos = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        thislen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].echo_id), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].layer_id), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].point_type), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].horizontal_angle), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].radial_distance), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].x), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].y), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].z), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].echo_pulse_width), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int __LidarSickPoint_decode_array_cleanup(LidarSickPoint* p, uint32_t elements)
{
    uint32_t element;
    for (element = 0; element < elements; ++element) {

        __int16_t_decode_array_cleanup(&(p[element].echo_id), 1);

        __int16_t_decode_array_cleanup(&(p[element].layer_id), 1);

        __int32_t_decode_array_cleanup(&(p[element].point_type), 1);

        __int32_t_decode_array_cleanup(&(p[element].horizontal_angle), 1);

        __double_decode_array_cleanup(&(p[element].radial_distance), 1);

        __double_decode_array_cleanup(&(p[element].x), 1);

        __double_decode_array_cleanup(&(p[element].y), 1);

        __double_decode_array_cleanup(&(p[element].z), 1);

        __double_decode_array_cleanup(&(p[element].echo_pulse_width), 1);

    }
    return 0;
}

int LidarSickPoint_decode(const void* buf, uint32_t offset, uint32_t maxlen, LidarSickPoint* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __LidarSickPoint_get_hash();

    int64_t this_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (this_hash != hash) return -1;

    thislen = __LidarSickPoint_decode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int LidarSickPoint_decode_cleanup(LidarSickPoint* p)
{
    return __LidarSickPoint_decode_array_cleanup(p, 1);
}

uint32_t __LidarSickPoint_clone_array(const LidarSickPoint* p, LidarSickPoint* q, uint32_t elements)
{
    uint32_t n = 0, element;
    for (element = 0; element < elements; ++element) {

        n += __int16_t_clone_array(&(p[element].echo_id), &(q[element].echo_id), 1);

        n += __int16_t_clone_array(&(p[element].layer_id), &(q[element].layer_id), 1);

        n += __int32_t_clone_array(&(p[element].point_type), &(q[element].point_type), 1);

        n += __int32_t_clone_array(&(p[element].horizontal_angle), &(q[element].horizontal_angle), 1);

        n += __double_clone_array(&(p[element].radial_distance), &(q[element].radial_distance), 1);

        n += __double_clone_array(&(p[element].x), &(q[element].x), 1);

        n += __double_clone_array(&(p[element].y), &(q[element].y), 1);

        n += __double_clone_array(&(p[element].z), &(q[element].z), 1);

        n += __double_clone_array(&(p[element].echo_pulse_width), &(q[element].echo_pulse_width), 1);

    }
    return n;
}

LidarSickPoint* LidarSickPoint_copy(const LidarSickPoint* p)
{
    LidarSickPoint* q = (LidarSickPoint*) malloc(sizeof(LidarSickPoint));
    __LidarSickPoint_clone_array(p, q, 1);
    return q;
}

void LidarSickPoint_destroy(LidarSickPoint* p)
{
    __LidarSickPoint_decode_array_cleanup(p, 1);
    free(p);
}

int LidarSickPoint_publish(zcm_t* zcm, const char* channel, const LidarSickPoint* p)
{
      uint32_t max_data_size = LidarSickPoint_encoded_size (p);
      uint8_t* buf = (uint8_t*) malloc (max_data_size);
      if (!buf) return -1;
      int data_size = LidarSickPoint_encode (buf, 0, max_data_size, p);
      if (data_size < 0) {
          free (buf);
          return data_size;
      }
      int status = zcm_publish (zcm, channel, buf, (uint32_t)data_size);
      free (buf);
      return status;
}

struct _LidarSickPoint_subscription_t {
    LidarSickPoint_handler_t user_handler;
    void* userdata;
    zcm_sub_t* z_sub;
};
static
void LidarSickPoint_handler_stub (const zcm_recv_buf_t* rbuf,
                            const char* channel, void* userdata)
{
    int status;
    LidarSickPoint p;
    memset(&p, 0, sizeof(LidarSickPoint));
    status = LidarSickPoint_decode (rbuf->data, 0, rbuf->data_size, &p);
    if (status < 0) {
        fprintf (stderr, "error %d decoding LidarSickPoint!!!\n", status);
        return;
    }

    LidarSickPoint_subscription_t* h = (LidarSickPoint_subscription_t*) userdata;
    h->user_handler (rbuf, channel, &p, h->userdata);

    LidarSickPoint_decode_cleanup (&p);
}

LidarSickPoint_subscription_t* LidarSickPoint_subscribe (zcm_t* zcm,
                    const char* channel,
                    LidarSickPoint_handler_t f, void* userdata)
{
    LidarSickPoint_subscription_t* n = (LidarSickPoint_subscription_t*)
                       malloc(sizeof(LidarSickPoint_subscription_t));
    n->user_handler = f;
    n->userdata = userdata;
    n->z_sub = zcm_subscribe (zcm, channel,
                              LidarSickPoint_handler_stub, n);
    if (n->z_sub == NULL) {
        fprintf (stderr,"couldn't reg LidarSickPoint ZCM handler!\n");
        free (n);
        return NULL;
    }
    return n;
}

int LidarSickPoint_unsubscribe(zcm_t* zcm, LidarSickPoint_subscription_t* hid)
{
    int status = zcm_unsubscribe (zcm, hid->z_sub);
    if (0 != status) {
        fprintf(stderr,
           "couldn't unsubscribe LidarSickPoint_handler %p!\n", hid);
        return -1;
    }
    free (hid);
    return 0;
}

