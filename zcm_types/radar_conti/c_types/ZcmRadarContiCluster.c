// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by zcm-gen

#include <string.h>
#include "ZcmRadarContiCluster.h"

static int __ZcmRadarContiCluster_hash_computed;
static uint64_t __ZcmRadarContiCluster_hash;

uint64_t __ZcmRadarContiCluster_hash_recursive(const __zcm_hash_ptr* p)
{
    const __zcm_hash_ptr* fp;
    for (fp = p; fp != NULL; fp = fp->parent)
        if (fp->v == __ZcmRadarContiCluster_get_hash)
            return 0;

    __zcm_hash_ptr cp;
    cp.parent =  p;
    cp.v = (void*)__ZcmRadarContiCluster_get_hash;
    (void) cp;

    uint64_t hash = (uint64_t)0xe0d2bb4ef4e457e5LL
         + __int16_t_hash_recursive(&cp)
         + __ZcmRadarContiClusterGeneral_hash_recursive(&cp)
         + __ZcmRadarContiClusterQualityInfo_hash_recursive(&cp)
        ;

    return (hash<<1) + ((hash>>63)&1);
}

int64_t __ZcmRadarContiCluster_get_hash(void)
{
    if (!__ZcmRadarContiCluster_hash_computed) {
        __ZcmRadarContiCluster_hash = (int64_t)__ZcmRadarContiCluster_hash_recursive(NULL);
        __ZcmRadarContiCluster_hash_computed = 1;
    }

    return __ZcmRadarContiCluster_hash;
}

int __ZcmRadarContiCluster_encode_array(void* buf, uint32_t offset, uint32_t maxlen, const ZcmRadarContiCluster* p, uint32_t elements)
{
    uint32_t pos = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        thislen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].id), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __ZcmRadarContiClusterGeneral_encode_array(buf, offset + pos, maxlen - pos, &(p[element].general), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __ZcmRadarContiClusterQualityInfo_encode_array(buf, offset + pos, maxlen - pos, &(p[element].quality), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int ZcmRadarContiCluster_encode(void* buf, uint32_t offset, uint32_t maxlen, const ZcmRadarContiCluster* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __ZcmRadarContiCluster_get_hash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    thislen = __ZcmRadarContiCluster_encode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

uint32_t __ZcmRadarContiCluster_encoded_array_size(const ZcmRadarContiCluster* p, uint32_t elements)
{
    uint32_t size = 0, element;
    for (element = 0; element < elements; ++element) {

        size += __int16_t_encoded_array_size(&(p[element].id), 1);

        size += __ZcmRadarContiClusterGeneral_encoded_array_size(&(p[element].general), 1);

        size += __ZcmRadarContiClusterQualityInfo_encoded_array_size(&(p[element].quality), 1);

    }
    return size;
}

uint32_t ZcmRadarContiCluster_encoded_size(const ZcmRadarContiCluster* p)
{
    return 8 + __ZcmRadarContiCluster_encoded_array_size(p, 1);
}

uint32_t ZcmRadarContiCluster_struct_size(void)
{
    return sizeof(ZcmRadarContiCluster);
}

uint32_t ZcmRadarContiCluster_num_fields(void)
{
    return 3;
}

int ZcmRadarContiCluster_get_field(const ZcmRadarContiCluster* p, uint32_t i, zcm_field_t* f)
{
    if (i >= ZcmRadarContiCluster_num_fields())
        return 1;
    
    switch (i) {
    
        case 0: {
            f->name = "id";
            f->type = ZCM_FIELD_INT16_T;
            f->typestr = "int16_t";
            f->num_dim = 0;
            f->data = (void*) &p->id;
            return 0;
        }
        
        case 1: {
            /* ZcmRadarContiClusterGeneral */
            f->name = "general";
            f->type = ZCM_FIELD_USER_TYPE;
            f->typestr = "ZcmRadarContiClusterGeneral";
            f->num_dim = 0;
            f->data = (void*) &p->general;
            return 0;
        }
        
        case 2: {
            /* ZcmRadarContiClusterQualityInfo */
            f->name = "quality";
            f->type = ZCM_FIELD_USER_TYPE;
            f->typestr = "ZcmRadarContiClusterQualityInfo";
            f->num_dim = 0;
            f->data = (void*) &p->quality;
            return 0;
        }
        
        default:
            return 1;
    }
}

const zcm_type_info_t* ZcmRadarContiCluster_get_type_info(void)
{
    static int init = 0;
    static zcm_type_info_t typeinfo;
    if (!init) {
        typeinfo.encode         = (zcm_encode_t) ZcmRadarContiCluster_encode;
        typeinfo.decode         = (zcm_decode_t) ZcmRadarContiCluster_decode;
        typeinfo.decode_cleanup = (zcm_decode_cleanup_t) ZcmRadarContiCluster_decode_cleanup;
        typeinfo.encoded_size   = (zcm_encoded_size_t) ZcmRadarContiCluster_encoded_size;
        typeinfo.struct_size    = (zcm_struct_size_t)  ZcmRadarContiCluster_struct_size;
        typeinfo.num_fields     = (zcm_num_fields_t) ZcmRadarContiCluster_num_fields;
        typeinfo.get_field      = (zcm_get_field_t) ZcmRadarContiCluster_get_field;
        typeinfo.get_hash       = (zcm_get_hash_t) __ZcmRadarContiCluster_get_hash;
    }
    
    return &typeinfo;
}
int __ZcmRadarContiCluster_decode_array(const void* buf, uint32_t offset, uint32_t maxlen, ZcmRadarContiCluster* p, uint32_t elements)
{
    uint32_t pos = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        thislen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].id), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __ZcmRadarContiClusterGeneral_decode_array(buf, offset + pos, maxlen - pos, &(p[element].general), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __ZcmRadarContiClusterQualityInfo_decode_array(buf, offset + pos, maxlen - pos, &(p[element].quality), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int __ZcmRadarContiCluster_decode_array_cleanup(ZcmRadarContiCluster* p, uint32_t elements)
{
    uint32_t element;
    for (element = 0; element < elements; ++element) {

        __int16_t_decode_array_cleanup(&(p[element].id), 1);

        __ZcmRadarContiClusterGeneral_decode_array_cleanup(&(p[element].general), 1);

        __ZcmRadarContiClusterQualityInfo_decode_array_cleanup(&(p[element].quality), 1);

    }
    return 0;
}

int ZcmRadarContiCluster_decode(const void* buf, uint32_t offset, uint32_t maxlen, ZcmRadarContiCluster* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __ZcmRadarContiCluster_get_hash();

    int64_t this_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (this_hash != hash) return -1;

    thislen = __ZcmRadarContiCluster_decode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int ZcmRadarContiCluster_decode_cleanup(ZcmRadarContiCluster* p)
{
    return __ZcmRadarContiCluster_decode_array_cleanup(p, 1);
}

uint32_t __ZcmRadarContiCluster_clone_array(const ZcmRadarContiCluster* p, ZcmRadarContiCluster* q, uint32_t elements)
{
    uint32_t n = 0, element;
    for (element = 0; element < elements; ++element) {

        n += __int16_t_clone_array(&(p[element].id), &(q[element].id), 1);

        n += __ZcmRadarContiClusterGeneral_clone_array(&(p[element].general), &(q[element].general), 1);

        n += __ZcmRadarContiClusterQualityInfo_clone_array(&(p[element].quality), &(q[element].quality), 1);

    }
    return n;
}

ZcmRadarContiCluster* ZcmRadarContiCluster_copy(const ZcmRadarContiCluster* p)
{
    ZcmRadarContiCluster* q = (ZcmRadarContiCluster*) malloc(sizeof(ZcmRadarContiCluster));
    __ZcmRadarContiCluster_clone_array(p, q, 1);
    return q;
}

void ZcmRadarContiCluster_destroy(ZcmRadarContiCluster* p)
{
    __ZcmRadarContiCluster_decode_array_cleanup(p, 1);
    free(p);
}

int ZcmRadarContiCluster_publish(zcm_t* zcm, const char* channel, const ZcmRadarContiCluster* p)
{
      uint32_t max_data_size = ZcmRadarContiCluster_encoded_size (p);
      uint8_t* buf = (uint8_t*) malloc (max_data_size);
      if (!buf) return -1;
      int data_size = ZcmRadarContiCluster_encode (buf, 0, max_data_size, p);
      if (data_size < 0) {
          free (buf);
          return data_size;
      }
      int status = zcm_publish (zcm, channel, buf, (uint32_t)data_size);
      free (buf);
      return status;
}

struct _ZcmRadarContiCluster_subscription_t {
    ZcmRadarContiCluster_handler_t user_handler;
    void* userdata;
    zcm_sub_t* z_sub;
};
static
void ZcmRadarContiCluster_handler_stub (const zcm_recv_buf_t* rbuf,
                            const char* channel, void* userdata)
{
    int status;
    ZcmRadarContiCluster p;
    memset(&p, 0, sizeof(ZcmRadarContiCluster));
    status = ZcmRadarContiCluster_decode (rbuf->data, 0, rbuf->data_size, &p);
    if (status < 0) {
        fprintf (stderr, "error %d decoding ZcmRadarContiCluster!!!\n", status);
        return;
    }

    ZcmRadarContiCluster_subscription_t* h = (ZcmRadarContiCluster_subscription_t*) userdata;
    h->user_handler (rbuf, channel, &p, h->userdata);

    ZcmRadarContiCluster_decode_cleanup (&p);
}

ZcmRadarContiCluster_subscription_t* ZcmRadarContiCluster_subscribe (zcm_t* zcm,
                    const char* channel,
                    ZcmRadarContiCluster_handler_t f, void* userdata)
{
    ZcmRadarContiCluster_subscription_t* n = (ZcmRadarContiCluster_subscription_t*)
                       malloc(sizeof(ZcmRadarContiCluster_subscription_t));
    n->user_handler = f;
    n->userdata = userdata;
    n->z_sub = zcm_subscribe (zcm, channel,
                              ZcmRadarContiCluster_handler_stub, n);
    if (n->z_sub == NULL) {
        fprintf (stderr,"couldn't reg ZcmRadarContiCluster ZCM handler!\n");
        free (n);
        return NULL;
    }
    return n;
}

int ZcmRadarContiCluster_unsubscribe(zcm_t* zcm, ZcmRadarContiCluster_subscription_t* hid)
{
    int status = zcm_unsubscribe (zcm, hid->z_sub);
    if (0 != status) {
        fprintf(stderr,
           "couldn't unsubscribe ZcmRadarContiCluster_handler %p!\n", hid);
        return -1;
    }
    free (hid);
    return 0;
}

