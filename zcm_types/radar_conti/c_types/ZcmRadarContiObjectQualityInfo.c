// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by zcm-gen

#include <string.h>
#include "ZcmRadarContiObjectQualityInfo.h"

static int __ZcmRadarContiObjectQualityInfo_hash_computed;
static uint64_t __ZcmRadarContiObjectQualityInfo_hash;

uint64_t __ZcmRadarContiObjectQualityInfo_hash_recursive(const __zcm_hash_ptr* p)
{
    const __zcm_hash_ptr* fp;
    for (fp = p; fp != NULL; fp = fp->parent)
        if (fp->v == __ZcmRadarContiObjectQualityInfo_get_hash)
            return 0;

    __zcm_hash_ptr cp;
    cp.parent =  p;
    cp.v = (void*)__ZcmRadarContiObjectQualityInfo_get_hash;
    (void) cp;

    uint64_t hash = (uint64_t)0xf5ca165979dfdb6cLL
         + __ZcmService_hash_recursive(&cp)
         + __int16_t_hash_recursive(&cp)
         + __float_hash_recursive(&cp)
         + __float_hash_recursive(&cp)
         + __float_hash_recursive(&cp)
         + __float_hash_recursive(&cp)
         + __float_hash_recursive(&cp)
         + __float_hash_recursive(&cp)
         + __float_hash_recursive(&cp)
         + __int8_t_hash_recursive(&cp)
         + __float_hash_recursive(&cp)
        ;

    return (hash<<1) + ((hash>>63)&1);
}

int64_t __ZcmRadarContiObjectQualityInfo_get_hash(void)
{
    if (!__ZcmRadarContiObjectQualityInfo_hash_computed) {
        __ZcmRadarContiObjectQualityInfo_hash = (int64_t)__ZcmRadarContiObjectQualityInfo_hash_recursive(NULL);
        __ZcmRadarContiObjectQualityInfo_hash_computed = 1;
    }

    return __ZcmRadarContiObjectQualityInfo_hash;
}

int __ZcmRadarContiObjectQualityInfo_encode_array(void* buf, uint32_t offset, uint32_t maxlen, const ZcmRadarContiObjectQualityInfo* p, uint32_t elements)
{
    uint32_t pos = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        thislen = __ZcmService_encode_array(buf, offset + pos, maxlen - pos, &(p[element].service), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].id), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __float_encode_array(buf, offset + pos, maxlen - pos, &(p[element].dist_long_rms), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __float_encode_array(buf, offset + pos, maxlen - pos, &(p[element].vrel_long_rms), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __float_encode_array(buf, offset + pos, maxlen - pos, &(p[element].dist_lat_rms), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __float_encode_array(buf, offset + pos, maxlen - pos, &(p[element].vrel_lat_rms), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __float_encode_array(buf, offset + pos, maxlen - pos, &(p[element].arel_lat_rms), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __float_encode_array(buf, offset + pos, maxlen - pos, &(p[element].areal_long_rms), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __float_encode_array(buf, offset + pos, maxlen - pos, &(p[element].orientation_rms), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].meas_state), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __float_encode_array(buf, offset + pos, maxlen - pos, &(p[element].prob_of_exist), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int ZcmRadarContiObjectQualityInfo_encode(void* buf, uint32_t offset, uint32_t maxlen, const ZcmRadarContiObjectQualityInfo* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __ZcmRadarContiObjectQualityInfo_get_hash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    thislen = __ZcmRadarContiObjectQualityInfo_encode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

uint32_t __ZcmRadarContiObjectQualityInfo_encoded_array_size(const ZcmRadarContiObjectQualityInfo* p, uint32_t elements)
{
    uint32_t size = 0, element;
    for (element = 0; element < elements; ++element) {

        size += __ZcmService_encoded_array_size(&(p[element].service), 1);

        size += __int16_t_encoded_array_size(&(p[element].id), 1);

        size += __float_encoded_array_size(&(p[element].dist_long_rms), 1);

        size += __float_encoded_array_size(&(p[element].vrel_long_rms), 1);

        size += __float_encoded_array_size(&(p[element].dist_lat_rms), 1);

        size += __float_encoded_array_size(&(p[element].vrel_lat_rms), 1);

        size += __float_encoded_array_size(&(p[element].arel_lat_rms), 1);

        size += __float_encoded_array_size(&(p[element].areal_long_rms), 1);

        size += __float_encoded_array_size(&(p[element].orientation_rms), 1);

        size += __int8_t_encoded_array_size(&(p[element].meas_state), 1);

        size += __float_encoded_array_size(&(p[element].prob_of_exist), 1);

    }
    return size;
}

uint32_t ZcmRadarContiObjectQualityInfo_encoded_size(const ZcmRadarContiObjectQualityInfo* p)
{
    return 8 + __ZcmRadarContiObjectQualityInfo_encoded_array_size(p, 1);
}

uint32_t ZcmRadarContiObjectQualityInfo_struct_size(void)
{
    return sizeof(ZcmRadarContiObjectQualityInfo);
}

uint32_t ZcmRadarContiObjectQualityInfo_num_fields(void)
{
    return 11;
}

int ZcmRadarContiObjectQualityInfo_get_field(const ZcmRadarContiObjectQualityInfo* p, uint32_t i, zcm_field_t* f)
{
    if (i >= ZcmRadarContiObjectQualityInfo_num_fields())
        return 1;
    
    switch (i) {
    
        case 0: {
            /* ZcmService */
            f->name = "service";
            f->type = ZCM_FIELD_USER_TYPE;
            f->typestr = "ZcmService";
            f->num_dim = 0;
            f->data = (void*) &p->service;
            return 0;
        }
        
        case 1: {
            f->name = "id";
            f->type = ZCM_FIELD_INT16_T;
            f->typestr = "int16_t";
            f->num_dim = 0;
            f->data = (void*) &p->id;
            return 0;
        }
        
        case 2: {
            f->name = "dist_long_rms";
            f->type = ZCM_FIELD_FLOAT;
            f->typestr = "float";
            f->num_dim = 0;
            f->data = (void*) &p->dist_long_rms;
            return 0;
        }
        
        case 3: {
            f->name = "vrel_long_rms";
            f->type = ZCM_FIELD_FLOAT;
            f->typestr = "float";
            f->num_dim = 0;
            f->data = (void*) &p->vrel_long_rms;
            return 0;
        }
        
        case 4: {
            f->name = "dist_lat_rms";
            f->type = ZCM_FIELD_FLOAT;
            f->typestr = "float";
            f->num_dim = 0;
            f->data = (void*) &p->dist_lat_rms;
            return 0;
        }
        
        case 5: {
            f->name = "vrel_lat_rms";
            f->type = ZCM_FIELD_FLOAT;
            f->typestr = "float";
            f->num_dim = 0;
            f->data = (void*) &p->vrel_lat_rms;
            return 0;
        }
        
        case 6: {
            f->name = "arel_lat_rms";
            f->type = ZCM_FIELD_FLOAT;
            f->typestr = "float";
            f->num_dim = 0;
            f->data = (void*) &p->arel_lat_rms;
            return 0;
        }
        
        case 7: {
            f->name = "areal_long_rms";
            f->type = ZCM_FIELD_FLOAT;
            f->typestr = "float";
            f->num_dim = 0;
            f->data = (void*) &p->areal_long_rms;
            return 0;
        }
        
        case 8: {
            f->name = "orientation_rms";
            f->type = ZCM_FIELD_FLOAT;
            f->typestr = "float";
            f->num_dim = 0;
            f->data = (void*) &p->orientation_rms;
            return 0;
        }
        
        case 9: {
            f->name = "meas_state";
            f->type = ZCM_FIELD_INT8_T;
            f->typestr = "int8_t";
            f->num_dim = 0;
            f->data = (void*) &p->meas_state;
            return 0;
        }
        
        case 10: {
            f->name = "prob_of_exist";
            f->type = ZCM_FIELD_FLOAT;
            f->typestr = "float";
            f->num_dim = 0;
            f->data = (void*) &p->prob_of_exist;
            return 0;
        }
        
        default:
            return 1;
    }
}

const zcm_type_info_t* ZcmRadarContiObjectQualityInfo_get_type_info(void)
{
    static int init = 0;
    static zcm_type_info_t typeinfo;
    if (!init) {
        typeinfo.encode         = (zcm_encode_t) ZcmRadarContiObjectQualityInfo_encode;
        typeinfo.decode         = (zcm_decode_t) ZcmRadarContiObjectQualityInfo_decode;
        typeinfo.decode_cleanup = (zcm_decode_cleanup_t) ZcmRadarContiObjectQualityInfo_decode_cleanup;
        typeinfo.encoded_size   = (zcm_encoded_size_t) ZcmRadarContiObjectQualityInfo_encoded_size;
        typeinfo.struct_size    = (zcm_struct_size_t)  ZcmRadarContiObjectQualityInfo_struct_size;
        typeinfo.num_fields     = (zcm_num_fields_t) ZcmRadarContiObjectQualityInfo_num_fields;
        typeinfo.get_field      = (zcm_get_field_t) ZcmRadarContiObjectQualityInfo_get_field;
        typeinfo.get_hash       = (zcm_get_hash_t) __ZcmRadarContiObjectQualityInfo_get_hash;
    }
    
    return &typeinfo;
}
int __ZcmRadarContiObjectQualityInfo_decode_array(const void* buf, uint32_t offset, uint32_t maxlen, ZcmRadarContiObjectQualityInfo* p, uint32_t elements)
{
    uint32_t pos = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        thislen = __ZcmService_decode_array(buf, offset + pos, maxlen - pos, &(p[element].service), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].id), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __float_decode_array(buf, offset + pos, maxlen - pos, &(p[element].dist_long_rms), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __float_decode_array(buf, offset + pos, maxlen - pos, &(p[element].vrel_long_rms), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __float_decode_array(buf, offset + pos, maxlen - pos, &(p[element].dist_lat_rms), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __float_decode_array(buf, offset + pos, maxlen - pos, &(p[element].vrel_lat_rms), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __float_decode_array(buf, offset + pos, maxlen - pos, &(p[element].arel_lat_rms), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __float_decode_array(buf, offset + pos, maxlen - pos, &(p[element].areal_long_rms), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __float_decode_array(buf, offset + pos, maxlen - pos, &(p[element].orientation_rms), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].meas_state), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __float_decode_array(buf, offset + pos, maxlen - pos, &(p[element].prob_of_exist), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int __ZcmRadarContiObjectQualityInfo_decode_array_cleanup(ZcmRadarContiObjectQualityInfo* p, uint32_t elements)
{
    uint32_t element;
    for (element = 0; element < elements; ++element) {

        __ZcmService_decode_array_cleanup(&(p[element].service), 1);

        __int16_t_decode_array_cleanup(&(p[element].id), 1);

        __float_decode_array_cleanup(&(p[element].dist_long_rms), 1);

        __float_decode_array_cleanup(&(p[element].vrel_long_rms), 1);

        __float_decode_array_cleanup(&(p[element].dist_lat_rms), 1);

        __float_decode_array_cleanup(&(p[element].vrel_lat_rms), 1);

        __float_decode_array_cleanup(&(p[element].arel_lat_rms), 1);

        __float_decode_array_cleanup(&(p[element].areal_long_rms), 1);

        __float_decode_array_cleanup(&(p[element].orientation_rms), 1);

        __int8_t_decode_array_cleanup(&(p[element].meas_state), 1);

        __float_decode_array_cleanup(&(p[element].prob_of_exist), 1);

    }
    return 0;
}

int ZcmRadarContiObjectQualityInfo_decode(const void* buf, uint32_t offset, uint32_t maxlen, ZcmRadarContiObjectQualityInfo* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __ZcmRadarContiObjectQualityInfo_get_hash();

    int64_t this_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (this_hash != hash) return -1;

    thislen = __ZcmRadarContiObjectQualityInfo_decode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int ZcmRadarContiObjectQualityInfo_decode_cleanup(ZcmRadarContiObjectQualityInfo* p)
{
    return __ZcmRadarContiObjectQualityInfo_decode_array_cleanup(p, 1);
}

uint32_t __ZcmRadarContiObjectQualityInfo_clone_array(const ZcmRadarContiObjectQualityInfo* p, ZcmRadarContiObjectQualityInfo* q, uint32_t elements)
{
    uint32_t n = 0, element;
    for (element = 0; element < elements; ++element) {

        n += __ZcmService_clone_array(&(p[element].service), &(q[element].service), 1);

        n += __int16_t_clone_array(&(p[element].id), &(q[element].id), 1);

        n += __float_clone_array(&(p[element].dist_long_rms), &(q[element].dist_long_rms), 1);

        n += __float_clone_array(&(p[element].vrel_long_rms), &(q[element].vrel_long_rms), 1);

        n += __float_clone_array(&(p[element].dist_lat_rms), &(q[element].dist_lat_rms), 1);

        n += __float_clone_array(&(p[element].vrel_lat_rms), &(q[element].vrel_lat_rms), 1);

        n += __float_clone_array(&(p[element].arel_lat_rms), &(q[element].arel_lat_rms), 1);

        n += __float_clone_array(&(p[element].areal_long_rms), &(q[element].areal_long_rms), 1);

        n += __float_clone_array(&(p[element].orientation_rms), &(q[element].orientation_rms), 1);

        n += __int8_t_clone_array(&(p[element].meas_state), &(q[element].meas_state), 1);

        n += __float_clone_array(&(p[element].prob_of_exist), &(q[element].prob_of_exist), 1);

    }
    return n;
}

ZcmRadarContiObjectQualityInfo* ZcmRadarContiObjectQualityInfo_copy(const ZcmRadarContiObjectQualityInfo* p)
{
    ZcmRadarContiObjectQualityInfo* q = (ZcmRadarContiObjectQualityInfo*) malloc(sizeof(ZcmRadarContiObjectQualityInfo));
    __ZcmRadarContiObjectQualityInfo_clone_array(p, q, 1);
    return q;
}

void ZcmRadarContiObjectQualityInfo_destroy(ZcmRadarContiObjectQualityInfo* p)
{
    __ZcmRadarContiObjectQualityInfo_decode_array_cleanup(p, 1);
    free(p);
}

int ZcmRadarContiObjectQualityInfo_publish(zcm_t* zcm, const char* channel, const ZcmRadarContiObjectQualityInfo* p)
{
      uint32_t max_data_size = ZcmRadarContiObjectQualityInfo_encoded_size (p);
      uint8_t* buf = (uint8_t*) malloc (max_data_size);
      if (!buf) return -1;
      int data_size = ZcmRadarContiObjectQualityInfo_encode (buf, 0, max_data_size, p);
      if (data_size < 0) {
          free (buf);
          return data_size;
      }
      int status = zcm_publish (zcm, channel, buf, (uint32_t)data_size);
      free (buf);
      return status;
}

struct _ZcmRadarContiObjectQualityInfo_subscription_t {
    ZcmRadarContiObjectQualityInfo_handler_t user_handler;
    void* userdata;
    zcm_sub_t* z_sub;
};
static
void ZcmRadarContiObjectQualityInfo_handler_stub (const zcm_recv_buf_t* rbuf,
                            const char* channel, void* userdata)
{
    int status;
    ZcmRadarContiObjectQualityInfo p;
    memset(&p, 0, sizeof(ZcmRadarContiObjectQualityInfo));
    status = ZcmRadarContiObjectQualityInfo_decode (rbuf->data, 0, rbuf->data_size, &p);
    if (status < 0) {
        fprintf (stderr, "error %d decoding ZcmRadarContiObjectQualityInfo!!!\n", status);
        return;
    }

    ZcmRadarContiObjectQualityInfo_subscription_t* h = (ZcmRadarContiObjectQualityInfo_subscription_t*) userdata;
    h->user_handler (rbuf, channel, &p, h->userdata);

    ZcmRadarContiObjectQualityInfo_decode_cleanup (&p);
}

ZcmRadarContiObjectQualityInfo_subscription_t* ZcmRadarContiObjectQualityInfo_subscribe (zcm_t* zcm,
                    const char* channel,
                    ZcmRadarContiObjectQualityInfo_handler_t f, void* userdata)
{
    ZcmRadarContiObjectQualityInfo_subscription_t* n = (ZcmRadarContiObjectQualityInfo_subscription_t*)
                       malloc(sizeof(ZcmRadarContiObjectQualityInfo_subscription_t));
    n->user_handler = f;
    n->userdata = userdata;
    n->z_sub = zcm_subscribe (zcm, channel,
                              ZcmRadarContiObjectQualityInfo_handler_stub, n);
    if (n->z_sub == NULL) {
        fprintf (stderr,"couldn't reg ZcmRadarContiObjectQualityInfo ZCM handler!\n");
        free (n);
        return NULL;
    }
    return n;
}

int ZcmRadarContiObjectQualityInfo_unsubscribe(zcm_t* zcm, ZcmRadarContiObjectQualityInfo_subscription_t* hid)
{
    int status = zcm_unsubscribe (zcm, hid->z_sub);
    if (0 != status) {
        fprintf(stderr,
           "couldn't unsubscribe ZcmRadarContiObjectQualityInfo_handler %p!\n", hid);
        return -1;
    }
    free (hid);
    return 0;
}

