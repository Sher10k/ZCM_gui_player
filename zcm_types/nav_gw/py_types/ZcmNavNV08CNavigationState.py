"""ZCM type definitions
This file automatically generated by zcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class ZcmNavNV08CNavigationState(object):
    __slots__ = ["solutionMark", "diffCorSign", "raimControl"]

    def __init__(self):
        self.solutionMark = 0
        self.diffCorSign = 0
        self.raimControl = 0

    def encode(self):
        buf = BytesIO()
        buf.write(ZcmNavNV08CNavigationState._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">bbb", self.solutionMark, self.diffCorSign, self.raimControl))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != ZcmNavNV08CNavigationState._get_packed_fingerprint():
            raise ValueError("Decode error")
        return ZcmNavNV08CNavigationState._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = ZcmNavNV08CNavigationState()
        self.solutionMark, self.diffCorSign, self.raimControl = struct.unpack(">bbb", buf.read(3))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if ZcmNavNV08CNavigationState in parents: return 0
        tmphash = (0x56bffc619bcbe9db) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + ((tmphash>>63)&0x1)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if ZcmNavNV08CNavigationState._packed_fingerprint is None:
            ZcmNavNV08CNavigationState._packed_fingerprint = struct.pack(">Q", ZcmNavNV08CNavigationState._get_hash_recursive([]))
        return ZcmNavNV08CNavigationState._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

