// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by zcm-gen

#include <string.h>
#include "ZcmNavNV08CVelocityVector.h"

static int __ZcmNavNV08CVelocityVector_hash_computed;
static uint64_t __ZcmNavNV08CVelocityVector_hash;

uint64_t __ZcmNavNV08CVelocityVector_hash_recursive(const __zcm_hash_ptr* p)
{
    const __zcm_hash_ptr* fp;
    for (fp = p; fp != NULL; fp = fp->parent)
        if (fp->v == __ZcmNavNV08CVelocityVector_get_hash)
            return 0;

    __zcm_hash_ptr cp;
    cp.parent =  p;
    cp.v = (void*)__ZcmNavNV08CVelocityVector_get_hash;
    (void) cp;

    uint64_t hash = (uint64_t)0x77848cd658e08678LL
         + __double_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
        ;

    return (hash<<1) + ((hash>>63)&1);
}

int64_t __ZcmNavNV08CVelocityVector_get_hash(void)
{
    if (!__ZcmNavNV08CVelocityVector_hash_computed) {
        __ZcmNavNV08CVelocityVector_hash = (int64_t)__ZcmNavNV08CVelocityVector_hash_recursive(NULL);
        __ZcmNavNV08CVelocityVector_hash_computed = 1;
    }

    return __ZcmNavNV08CVelocityVector_hash;
}

int __ZcmNavNV08CVelocityVector_encode_array(void* buf, uint32_t offset, uint32_t maxlen, const ZcmNavNV08CVelocityVector* p, uint32_t elements)
{
    uint32_t pos = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].XSpeed), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].YSpeed), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].ZSpeed), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int ZcmNavNV08CVelocityVector_encode(void* buf, uint32_t offset, uint32_t maxlen, const ZcmNavNV08CVelocityVector* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __ZcmNavNV08CVelocityVector_get_hash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    thislen = __ZcmNavNV08CVelocityVector_encode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

uint32_t __ZcmNavNV08CVelocityVector_encoded_array_size(const ZcmNavNV08CVelocityVector* p, uint32_t elements)
{
    uint32_t size = 0, element;
    for (element = 0; element < elements; ++element) {

        size += __double_encoded_array_size(&(p[element].XSpeed), 1);

        size += __double_encoded_array_size(&(p[element].YSpeed), 1);

        size += __double_encoded_array_size(&(p[element].ZSpeed), 1);

    }
    return size;
}

uint32_t ZcmNavNV08CVelocityVector_encoded_size(const ZcmNavNV08CVelocityVector* p)
{
    return 8 + __ZcmNavNV08CVelocityVector_encoded_array_size(p, 1);
}

uint32_t ZcmNavNV08CVelocityVector_struct_size(void)
{
    return sizeof(ZcmNavNV08CVelocityVector);
}

uint32_t ZcmNavNV08CVelocityVector_num_fields(void)
{
    return 3;
}

int ZcmNavNV08CVelocityVector_get_field(const ZcmNavNV08CVelocityVector* p, uint32_t i, zcm_field_t* f)
{
    if (i >= ZcmNavNV08CVelocityVector_num_fields())
        return 1;
    
    switch (i) {
    
        case 0: {
            f->name = "XSpeed";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 0;
            f->data = (void*) &p->XSpeed;
            return 0;
        }
        
        case 1: {
            f->name = "YSpeed";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 0;
            f->data = (void*) &p->YSpeed;
            return 0;
        }
        
        case 2: {
            f->name = "ZSpeed";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 0;
            f->data = (void*) &p->ZSpeed;
            return 0;
        }
        
        default:
            return 1;
    }
}

const zcm_type_info_t* ZcmNavNV08CVelocityVector_get_type_info(void)
{
    static int init = 0;
    static zcm_type_info_t typeinfo;
    if (!init) {
        typeinfo.encode         = (zcm_encode_t) ZcmNavNV08CVelocityVector_encode;
        typeinfo.decode         = (zcm_decode_t) ZcmNavNV08CVelocityVector_decode;
        typeinfo.decode_cleanup = (zcm_decode_cleanup_t) ZcmNavNV08CVelocityVector_decode_cleanup;
        typeinfo.encoded_size   = (zcm_encoded_size_t) ZcmNavNV08CVelocityVector_encoded_size;
        typeinfo.struct_size    = (zcm_struct_size_t)  ZcmNavNV08CVelocityVector_struct_size;
        typeinfo.num_fields     = (zcm_num_fields_t) ZcmNavNV08CVelocityVector_num_fields;
        typeinfo.get_field      = (zcm_get_field_t) ZcmNavNV08CVelocityVector_get_field;
        typeinfo.get_hash       = (zcm_get_hash_t) __ZcmNavNV08CVelocityVector_get_hash;
    }
    
    return &typeinfo;
}
int __ZcmNavNV08CVelocityVector_decode_array(const void* buf, uint32_t offset, uint32_t maxlen, ZcmNavNV08CVelocityVector* p, uint32_t elements)
{
    uint32_t pos = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].XSpeed), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].YSpeed), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].ZSpeed), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int __ZcmNavNV08CVelocityVector_decode_array_cleanup(ZcmNavNV08CVelocityVector* p, uint32_t elements)
{
    uint32_t element;
    for (element = 0; element < elements; ++element) {

        __double_decode_array_cleanup(&(p[element].XSpeed), 1);

        __double_decode_array_cleanup(&(p[element].YSpeed), 1);

        __double_decode_array_cleanup(&(p[element].ZSpeed), 1);

    }
    return 0;
}

int ZcmNavNV08CVelocityVector_decode(const void* buf, uint32_t offset, uint32_t maxlen, ZcmNavNV08CVelocityVector* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __ZcmNavNV08CVelocityVector_get_hash();

    int64_t this_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (this_hash != hash) return -1;

    thislen = __ZcmNavNV08CVelocityVector_decode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int ZcmNavNV08CVelocityVector_decode_cleanup(ZcmNavNV08CVelocityVector* p)
{
    return __ZcmNavNV08CVelocityVector_decode_array_cleanup(p, 1);
}

uint32_t __ZcmNavNV08CVelocityVector_clone_array(const ZcmNavNV08CVelocityVector* p, ZcmNavNV08CVelocityVector* q, uint32_t elements)
{
    uint32_t n = 0, element;
    for (element = 0; element < elements; ++element) {

        n += __double_clone_array(&(p[element].XSpeed), &(q[element].XSpeed), 1);

        n += __double_clone_array(&(p[element].YSpeed), &(q[element].YSpeed), 1);

        n += __double_clone_array(&(p[element].ZSpeed), &(q[element].ZSpeed), 1);

    }
    return n;
}

ZcmNavNV08CVelocityVector* ZcmNavNV08CVelocityVector_copy(const ZcmNavNV08CVelocityVector* p)
{
    ZcmNavNV08CVelocityVector* q = (ZcmNavNV08CVelocityVector*) malloc(sizeof(ZcmNavNV08CVelocityVector));
    __ZcmNavNV08CVelocityVector_clone_array(p, q, 1);
    return q;
}

void ZcmNavNV08CVelocityVector_destroy(ZcmNavNV08CVelocityVector* p)
{
    __ZcmNavNV08CVelocityVector_decode_array_cleanup(p, 1);
    free(p);
}

int ZcmNavNV08CVelocityVector_publish(zcm_t* zcm, const char* channel, const ZcmNavNV08CVelocityVector* p)
{
      uint32_t max_data_size = ZcmNavNV08CVelocityVector_encoded_size (p);
      uint8_t* buf = (uint8_t*) malloc (max_data_size);
      if (!buf) return -1;
      int data_size = ZcmNavNV08CVelocityVector_encode (buf, 0, max_data_size, p);
      if (data_size < 0) {
          free (buf);
          return data_size;
      }
      int status = zcm_publish (zcm, channel, buf, (uint32_t)data_size);
      free (buf);
      return status;
}

struct _ZcmNavNV08CVelocityVector_subscription_t {
    ZcmNavNV08CVelocityVector_handler_t user_handler;
    void* userdata;
    zcm_sub_t* z_sub;
};
static
void ZcmNavNV08CVelocityVector_handler_stub (const zcm_recv_buf_t* rbuf,
                            const char* channel, void* userdata)
{
    int status;
    ZcmNavNV08CVelocityVector p;
    memset(&p, 0, sizeof(ZcmNavNV08CVelocityVector));
    status = ZcmNavNV08CVelocityVector_decode (rbuf->data, 0, rbuf->data_size, &p);
    if (status < 0) {
        fprintf (stderr, "error %d decoding ZcmNavNV08CVelocityVector!!!\n", status);
        return;
    }

    ZcmNavNV08CVelocityVector_subscription_t* h = (ZcmNavNV08CVelocityVector_subscription_t*) userdata;
    h->user_handler (rbuf, channel, &p, h->userdata);

    ZcmNavNV08CVelocityVector_decode_cleanup (&p);
}

ZcmNavNV08CVelocityVector_subscription_t* ZcmNavNV08CVelocityVector_subscribe (zcm_t* zcm,
                    const char* channel,
                    ZcmNavNV08CVelocityVector_handler_t f, void* userdata)
{
    ZcmNavNV08CVelocityVector_subscription_t* n = (ZcmNavNV08CVelocityVector_subscription_t*)
                       malloc(sizeof(ZcmNavNV08CVelocityVector_subscription_t));
    n->user_handler = f;
    n->userdata = userdata;
    n->z_sub = zcm_subscribe (zcm, channel,
                              ZcmNavNV08CVelocityVector_handler_stub, n);
    if (n->z_sub == NULL) {
        fprintf (stderr,"couldn't reg ZcmNavNV08CVelocityVector ZCM handler!\n");
        free (n);
        return NULL;
    }
    return n;
}

int ZcmNavNV08CVelocityVector_unsubscribe(zcm_t* zcm, ZcmNavNV08CVelocityVector_subscription_t* hid)
{
    int status = zcm_unsubscribe (zcm, hid->z_sub);
    if (0 != status) {
        fprintf(stderr,
           "couldn't unsubscribe ZcmNavNV08CVelocityVector_handler %p!\n", hid);
        return -1;
    }
    free (hid);
    return 0;
}

