// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by zcm-gen

#include <string.h>
#include "ZcmNavNV08CNavigationState.h"

static int __ZcmNavNV08CNavigationState_hash_computed;
static uint64_t __ZcmNavNV08CNavigationState_hash;

uint64_t __ZcmNavNV08CNavigationState_hash_recursive(const __zcm_hash_ptr* p)
{
    const __zcm_hash_ptr* fp;
    for (fp = p; fp != NULL; fp = fp->parent)
        if (fp->v == __ZcmNavNV08CNavigationState_get_hash)
            return 0;

    __zcm_hash_ptr cp;
    cp.parent =  p;
    cp.v = (void*)__ZcmNavNV08CNavigationState_get_hash;
    (void) cp;

    uint64_t hash = (uint64_t)0x56bffc619bcbe9dbLL
         + __int8_t_hash_recursive(&cp)
         + __int8_t_hash_recursive(&cp)
         + __int8_t_hash_recursive(&cp)
        ;

    return (hash<<1) + ((hash>>63)&1);
}

int64_t __ZcmNavNV08CNavigationState_get_hash(void)
{
    if (!__ZcmNavNV08CNavigationState_hash_computed) {
        __ZcmNavNV08CNavigationState_hash = (int64_t)__ZcmNavNV08CNavigationState_hash_recursive(NULL);
        __ZcmNavNV08CNavigationState_hash_computed = 1;
    }

    return __ZcmNavNV08CNavigationState_hash;
}

int __ZcmNavNV08CNavigationState_encode_array(void* buf, uint32_t offset, uint32_t maxlen, const ZcmNavNV08CNavigationState* p, uint32_t elements)
{
    uint32_t pos = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        thislen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].solutionMark), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].diffCorSign), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].raimControl), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int ZcmNavNV08CNavigationState_encode(void* buf, uint32_t offset, uint32_t maxlen, const ZcmNavNV08CNavigationState* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __ZcmNavNV08CNavigationState_get_hash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    thislen = __ZcmNavNV08CNavigationState_encode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

uint32_t __ZcmNavNV08CNavigationState_encoded_array_size(const ZcmNavNV08CNavigationState* p, uint32_t elements)
{
    uint32_t size = 0, element;
    for (element = 0; element < elements; ++element) {

        size += __int8_t_encoded_array_size(&(p[element].solutionMark), 1);

        size += __int8_t_encoded_array_size(&(p[element].diffCorSign), 1);

        size += __int8_t_encoded_array_size(&(p[element].raimControl), 1);

    }
    return size;
}

uint32_t ZcmNavNV08CNavigationState_encoded_size(const ZcmNavNV08CNavigationState* p)
{
    return 8 + __ZcmNavNV08CNavigationState_encoded_array_size(p, 1);
}

uint32_t ZcmNavNV08CNavigationState_struct_size(void)
{
    return sizeof(ZcmNavNV08CNavigationState);
}

uint32_t ZcmNavNV08CNavigationState_num_fields(void)
{
    return 3;
}

int ZcmNavNV08CNavigationState_get_field(const ZcmNavNV08CNavigationState* p, uint32_t i, zcm_field_t* f)
{
    if (i >= ZcmNavNV08CNavigationState_num_fields())
        return 1;
    
    switch (i) {
    
        case 0: {
            f->name = "solutionMark";
            f->type = ZCM_FIELD_INT8_T;
            f->typestr = "int8_t";
            f->num_dim = 0;
            f->data = (void*) &p->solutionMark;
            return 0;
        }
        
        case 1: {
            f->name = "diffCorSign";
            f->type = ZCM_FIELD_INT8_T;
            f->typestr = "int8_t";
            f->num_dim = 0;
            f->data = (void*) &p->diffCorSign;
            return 0;
        }
        
        case 2: {
            f->name = "raimControl";
            f->type = ZCM_FIELD_INT8_T;
            f->typestr = "int8_t";
            f->num_dim = 0;
            f->data = (void*) &p->raimControl;
            return 0;
        }
        
        default:
            return 1;
    }
}

const zcm_type_info_t* ZcmNavNV08CNavigationState_get_type_info(void)
{
    static int init = 0;
    static zcm_type_info_t typeinfo;
    if (!init) {
        typeinfo.encode         = (zcm_encode_t) ZcmNavNV08CNavigationState_encode;
        typeinfo.decode         = (zcm_decode_t) ZcmNavNV08CNavigationState_decode;
        typeinfo.decode_cleanup = (zcm_decode_cleanup_t) ZcmNavNV08CNavigationState_decode_cleanup;
        typeinfo.encoded_size   = (zcm_encoded_size_t) ZcmNavNV08CNavigationState_encoded_size;
        typeinfo.struct_size    = (zcm_struct_size_t)  ZcmNavNV08CNavigationState_struct_size;
        typeinfo.num_fields     = (zcm_num_fields_t) ZcmNavNV08CNavigationState_num_fields;
        typeinfo.get_field      = (zcm_get_field_t) ZcmNavNV08CNavigationState_get_field;
        typeinfo.get_hash       = (zcm_get_hash_t) __ZcmNavNV08CNavigationState_get_hash;
    }
    
    return &typeinfo;
}
int __ZcmNavNV08CNavigationState_decode_array(const void* buf, uint32_t offset, uint32_t maxlen, ZcmNavNV08CNavigationState* p, uint32_t elements)
{
    uint32_t pos = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        thislen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].solutionMark), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].diffCorSign), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].raimControl), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int __ZcmNavNV08CNavigationState_decode_array_cleanup(ZcmNavNV08CNavigationState* p, uint32_t elements)
{
    uint32_t element;
    for (element = 0; element < elements; ++element) {

        __int8_t_decode_array_cleanup(&(p[element].solutionMark), 1);

        __int8_t_decode_array_cleanup(&(p[element].diffCorSign), 1);

        __int8_t_decode_array_cleanup(&(p[element].raimControl), 1);

    }
    return 0;
}

int ZcmNavNV08CNavigationState_decode(const void* buf, uint32_t offset, uint32_t maxlen, ZcmNavNV08CNavigationState* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __ZcmNavNV08CNavigationState_get_hash();

    int64_t this_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (this_hash != hash) return -1;

    thislen = __ZcmNavNV08CNavigationState_decode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int ZcmNavNV08CNavigationState_decode_cleanup(ZcmNavNV08CNavigationState* p)
{
    return __ZcmNavNV08CNavigationState_decode_array_cleanup(p, 1);
}

uint32_t __ZcmNavNV08CNavigationState_clone_array(const ZcmNavNV08CNavigationState* p, ZcmNavNV08CNavigationState* q, uint32_t elements)
{
    uint32_t n = 0, element;
    for (element = 0; element < elements; ++element) {

        n += __int8_t_clone_array(&(p[element].solutionMark), &(q[element].solutionMark), 1);

        n += __int8_t_clone_array(&(p[element].diffCorSign), &(q[element].diffCorSign), 1);

        n += __int8_t_clone_array(&(p[element].raimControl), &(q[element].raimControl), 1);

    }
    return n;
}

ZcmNavNV08CNavigationState* ZcmNavNV08CNavigationState_copy(const ZcmNavNV08CNavigationState* p)
{
    ZcmNavNV08CNavigationState* q = (ZcmNavNV08CNavigationState*) malloc(sizeof(ZcmNavNV08CNavigationState));
    __ZcmNavNV08CNavigationState_clone_array(p, q, 1);
    return q;
}

void ZcmNavNV08CNavigationState_destroy(ZcmNavNV08CNavigationState* p)
{
    __ZcmNavNV08CNavigationState_decode_array_cleanup(p, 1);
    free(p);
}

int ZcmNavNV08CNavigationState_publish(zcm_t* zcm, const char* channel, const ZcmNavNV08CNavigationState* p)
{
      uint32_t max_data_size = ZcmNavNV08CNavigationState_encoded_size (p);
      uint8_t* buf = (uint8_t*) malloc (max_data_size);
      if (!buf) return -1;
      int data_size = ZcmNavNV08CNavigationState_encode (buf, 0, max_data_size, p);
      if (data_size < 0) {
          free (buf);
          return data_size;
      }
      int status = zcm_publish (zcm, channel, buf, (uint32_t)data_size);
      free (buf);
      return status;
}

struct _ZcmNavNV08CNavigationState_subscription_t {
    ZcmNavNV08CNavigationState_handler_t user_handler;
    void* userdata;
    zcm_sub_t* z_sub;
};
static
void ZcmNavNV08CNavigationState_handler_stub (const zcm_recv_buf_t* rbuf,
                            const char* channel, void* userdata)
{
    int status;
    ZcmNavNV08CNavigationState p;
    memset(&p, 0, sizeof(ZcmNavNV08CNavigationState));
    status = ZcmNavNV08CNavigationState_decode (rbuf->data, 0, rbuf->data_size, &p);
    if (status < 0) {
        fprintf (stderr, "error %d decoding ZcmNavNV08CNavigationState!!!\n", status);
        return;
    }

    ZcmNavNV08CNavigationState_subscription_t* h = (ZcmNavNV08CNavigationState_subscription_t*) userdata;
    h->user_handler (rbuf, channel, &p, h->userdata);

    ZcmNavNV08CNavigationState_decode_cleanup (&p);
}

ZcmNavNV08CNavigationState_subscription_t* ZcmNavNV08CNavigationState_subscribe (zcm_t* zcm,
                    const char* channel,
                    ZcmNavNV08CNavigationState_handler_t f, void* userdata)
{
    ZcmNavNV08CNavigationState_subscription_t* n = (ZcmNavNV08CNavigationState_subscription_t*)
                       malloc(sizeof(ZcmNavNV08CNavigationState_subscription_t));
    n->user_handler = f;
    n->userdata = userdata;
    n->z_sub = zcm_subscribe (zcm, channel,
                              ZcmNavNV08CNavigationState_handler_stub, n);
    if (n->z_sub == NULL) {
        fprintf (stderr,"couldn't reg ZcmNavNV08CNavigationState ZCM handler!\n");
        free (n);
        return NULL;
    }
    return n;
}

int ZcmNavNV08CNavigationState_unsubscribe(zcm_t* zcm, ZcmNavNV08CNavigationState_subscription_t* hid)
{
    int status = zcm_unsubscribe (zcm, hid->z_sub);
    if (0 != status) {
        fprintf(stderr,
           "couldn't unsubscribe ZcmNavNV08CNavigationState_handler %p!\n", hid);
        return -1;
    }
    free (hid);
    return 0;
}

