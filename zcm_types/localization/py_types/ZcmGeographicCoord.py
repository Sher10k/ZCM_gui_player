"""ZCM type definitions
This file automatically generated by zcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class ZcmGeographicCoord(object):
    __slots__ = ["lat", "lon"]

    def __init__(self):
        self.lat = 0.0
        self.lon = 0.0

    def encode(self):
        buf = BytesIO()
        buf.write(ZcmGeographicCoord._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">dd", self.lat, self.lon))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != ZcmGeographicCoord._get_packed_fingerprint():
            raise ValueError("Decode error")
        return ZcmGeographicCoord._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = ZcmGeographicCoord()
        self.lat, self.lon = struct.unpack(">dd", buf.read(16))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if ZcmGeographicCoord in parents: return 0
        tmphash = (0x13648b72dd05ba4b) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + ((tmphash>>63)&0x1)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if ZcmGeographicCoord._packed_fingerprint is None:
            ZcmGeographicCoord._packed_fingerprint = struct.pack(">Q", ZcmGeographicCoord._get_hash_recursive([]))
        return ZcmGeographicCoord._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

