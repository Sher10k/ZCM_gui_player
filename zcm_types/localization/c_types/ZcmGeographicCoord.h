// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by zcm-gen

#include <stdint.h>
#include <stdlib.h>
#include <zcm/zcm_coretypes.h>
#include <zcm/zcm.h>

#ifndef _ZcmGeographicCoord_h
#define _ZcmGeographicCoord_h

#ifdef __cplusplus
extern "C" {
#endif

/**
 * @struct ZcmGeographicCoord
 * @brief Координата в географической системе
 * @var ZcmGeographicCoord::lat
 * @brief - широта, градус
 * @var ZcmGeographicCoord::lon
 * @brief - долгота, градус
 *
 */
typedef struct _ZcmGeographicCoord ZcmGeographicCoord;
struct _ZcmGeographicCoord
{
    double     lat;
    double     lon;
};

/**
 * Create a deep copy of a ZcmGeographicCoord.
 * When no longer needed, destroy it with ZcmGeographicCoord_destroy()
 */
ZcmGeographicCoord* ZcmGeographicCoord_copy(const ZcmGeographicCoord* to_copy);

/**
 * Destroy an instance of ZcmGeographicCoord created by ZcmGeographicCoord_copy()
 */
void ZcmGeographicCoord_destroy(ZcmGeographicCoord* to_destroy);

/**
 * Identifies a single subscription.  This is an opaque data type.
 */
typedef struct _ZcmGeographicCoord_subscription_t ZcmGeographicCoord_subscription_t;

/**
 * Prototype for a callback function invoked when a message of type
 * ZcmGeographicCoord is received.
 */
typedef void(*ZcmGeographicCoord_handler_t)(const zcm_recv_buf_t* rbuf,
             const char* channel, const ZcmGeographicCoord* msg, void* userdata);

/**
 * Publish a message of type ZcmGeographicCoord using ZCM.
 *
 * @param zcm The ZCM instance to publish with.
 * @param channel The channel to publish on.
 * @param msg The message to publish.
 * @return 0 on success, <0 on error.  Success means ZCM has transferred
 * responsibility of the message data to the OS.
 */
int ZcmGeographicCoord_publish(zcm_t* zcm, const char* channel, const ZcmGeographicCoord* msg);

/**
 * Subscribe to messages of type ZcmGeographicCoord using ZCM.
 *
 * @param zcm The ZCM instance to subscribe with.
 * @param channel The channel to subscribe to.
 * @param handler The callback function invoked by ZCM when a message is received.
 *                This function is invoked by ZCM during calls to zcm_handle() and
 *                zcm_handle_timeout().
 * @param userdata An opaque pointer passed to @p handler when it is invoked.
 * @return pointer to subscription type, NULL if failure. Must clean up
 *         dynamic memory by passing the pointer to ZcmGeographicCoord_unsubscribe.
 */
ZcmGeographicCoord_subscription_t* ZcmGeographicCoord_subscribe(zcm_t* zcm, const char* channel, ZcmGeographicCoord_handler_t handler, void* userdata);

/**
 * Removes and destroys a subscription created by ZcmGeographicCoord_subscribe()
 */
int ZcmGeographicCoord_unsubscribe(zcm_t* zcm, ZcmGeographicCoord_subscription_t* hid);
/**
 * Encode a message of type ZcmGeographicCoord into binary form.
 *
 * @param buf The output buffer.
 * @param offset Encoding starts at this byte offset into @p buf.
 * @param maxlen Maximum number of bytes to write.  This should generally
 *               be equal to ZcmGeographicCoord_encoded_size().
 * @param msg The message to encode.
 * @return The number of bytes encoded, or <0 if an error occured.
 */
int ZcmGeographicCoord_encode(void* buf, uint32_t offset, uint32_t maxlen, const ZcmGeographicCoord* p);

/**
 * Decode a message of type ZcmGeographicCoord from binary form.
 * When decoding messages containing strings or variable-length arrays, this
 * function may allocate memory.  When finished with the decoded message,
 * release allocated resources with ZcmGeographicCoord_decode_cleanup().
 *
 * @param buf The buffer containing the encoded message
 * @param offset The byte offset into @p buf where the encoded message starts.
 * @param maxlen The maximum number of bytes to read while decoding.
 * @param msg Output parameter where the decoded message is stored
 * @return The number of bytes decoded, or <0 if an error occured.
 */
int ZcmGeographicCoord_decode(const void* buf, uint32_t offset, uint32_t maxlen, ZcmGeographicCoord* msg);

/**
 * Release resources allocated by ZcmGeographicCoord_decode()
 * @return 0
 */
int ZcmGeographicCoord_decode_cleanup(ZcmGeographicCoord* p);

/**
 * Check how many bytes are required to encode a message of type ZcmGeographicCoord
 */
uint32_t ZcmGeographicCoord_encoded_size(const ZcmGeographicCoord* p);
uint32_t ZcmGeographicCoord_struct_size(void);
uint32_t ZcmGeographicCoord_num_fields(void);
int      ZcmGeographicCoord_get_field(const ZcmGeographicCoord* p, uint32_t i, zcm_field_t* f);
const zcm_type_info_t* ZcmGeographicCoord_get_type_info(void);

// ZCM support functions. Users should not call these
int64_t  __ZcmGeographicCoord_get_hash(void);
uint64_t __ZcmGeographicCoord_hash_recursive(const __zcm_hash_ptr* p);
int      __ZcmGeographicCoord_encode_array(void* buf, uint32_t offset, uint32_t maxlen, const ZcmGeographicCoord* p, uint32_t elements);
int      __ZcmGeographicCoord_decode_array(const void* buf, uint32_t offset, uint32_t maxlen, ZcmGeographicCoord* p, uint32_t elements);
int      __ZcmGeographicCoord_decode_array_cleanup(ZcmGeographicCoord* p, uint32_t elements);
uint32_t __ZcmGeographicCoord_encoded_array_size(const ZcmGeographicCoord* p, uint32_t elements);
uint32_t __ZcmGeographicCoord_clone_array(const ZcmGeographicCoord* p, ZcmGeographicCoord* q, uint32_t elements);

#ifdef __cplusplus
}
#endif

#endif
