/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by zcm-gen
 **/

#include <zcm/zcm_coretypes.h>

#ifndef __ZcmObstacle_hpp__
#define __ZcmObstacle_hpp__

#include "ZcmObstacleProperties.hpp"
#include "ZcmObstacleLocalCoordinates.hpp"
#include "ZcmObstacleUtmCoordinates.hpp"
#include "ZcmObstacleGeoCoordinates.hpp"


/**
 * @struct ZcmObstacle
 * @brief  Данные об объекте
 * @var ZcmObstacle::cluster_id 
 * @brief - номер объекта
 * @var ZcmObstacle::object_type
 * @brief - тип объекта
 * @var ZcmObstacle::object_properies
 * @brief - свойства объекта
 * @var ZcmObstacle::local_coordinates
 * @brief - координаты в локальной системе
 * @var ZcmObstacle::utm_coordinates
 * @brief - координаты в UTM
 * @var ZcmObstacle::geo_coordinates
 * @brief - географические координаты
 *
 */
class ZcmObstacle
{
    public:
        int32_t    cluster_id;

        int32_t    object_type;

        ZcmObstacleProperties object_properies;

        ZcmObstacleLocalCoordinates local_coordinates;

        ZcmObstacleUtmCoordinates utm_coordinates;

        ZcmObstacleGeoCoordinates geo_coordinates;

    public:
        #if __cplusplus > 199711L /* if c++11 */
        static constexpr int8_t   STATIC_OBJECT = 0;
        static constexpr int8_t   MOVING_OBJECT = 1;
        #else
        static const     int8_t   STATIC_OBJECT = 0;
        static const     int8_t   MOVING_OBJECT = 1;
        #endif

    public:
        /**
         * Destructs a message properly if anything inherits from it
        */
        virtual ~ZcmObstacle() {}

        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void* buf, uint32_t offset, uint32_t maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline uint32_t getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to reqad while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void* buf, uint32_t offset, uint32_t maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "ZcmObstacle"
         */
        inline static const char* getTypeName();

        // ZCM support functions. Users should not call these
        inline int      _encodeNoHash(void* buf, uint32_t offset, uint32_t maxlen) const;
        inline uint32_t _getEncodedSizeNoHash() const;
        inline int      _decodeNoHash(const void* buf, uint32_t offset, uint32_t maxlen);
        inline static uint64_t _computeHash(const __zcm_hash_ptr* p);
};

int ZcmObstacle::encode(void* buf, uint32_t offset, uint32_t maxlen) const
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = (int64_t)getHash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int ZcmObstacle::decode(const void* buf, uint32_t offset, uint32_t maxlen)
{
    uint32_t pos = 0;
    int thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

uint32_t ZcmObstacle::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t ZcmObstacle::getHash()
{
    static int64_t hash = _computeHash(NULL);
    return hash;
}

const char* ZcmObstacle::getTypeName()
{
    return "ZcmObstacle";
}

int ZcmObstacle::_encodeNoHash(void* buf, uint32_t offset, uint32_t maxlen) const
{
    uint32_t pos = 0;
    int thislen;

    thislen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->cluster_id, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->object_type, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = this->object_properies._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = this->local_coordinates._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = this->utm_coordinates._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = this->geo_coordinates._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int ZcmObstacle::_decodeNoHash(const void* buf, uint32_t offset, uint32_t maxlen)
{
    uint32_t pos = 0;
    int thislen;

    thislen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->cluster_id, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->object_type, 1);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = this->object_properies._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = this->local_coordinates._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = this->utm_coordinates._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(thislen < 0) return thislen; else pos += thislen;

    thislen = this->geo_coordinates._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(thislen < 0) return thislen; else pos += thislen;

    return pos;
}

uint32_t ZcmObstacle::_getEncodedSizeNoHash() const
{
    uint32_t enc_size = 0;
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += this->object_properies._getEncodedSizeNoHash();
    enc_size += this->local_coordinates._getEncodedSizeNoHash();
    enc_size += this->utm_coordinates._getEncodedSizeNoHash();
    enc_size += this->geo_coordinates._getEncodedSizeNoHash();
    return enc_size;
}

uint64_t ZcmObstacle::_computeHash(const __zcm_hash_ptr* p)
{
    const __zcm_hash_ptr* fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == ZcmObstacle::getHash)
            return 0;
    const __zcm_hash_ptr cp = { p, (void*)ZcmObstacle::getHash };

    uint64_t hash = (uint64_t)0xbd77d4f68b2e8a0fLL +
         ZcmObstacleProperties::_computeHash(&cp) +
         ZcmObstacleLocalCoordinates::_computeHash(&cp) +
         ZcmObstacleUtmCoordinates::_computeHash(&cp) +
         ZcmObstacleGeoCoordinates::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

#endif
