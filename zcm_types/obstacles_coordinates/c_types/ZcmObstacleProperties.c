// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by zcm-gen

#include <string.h>
#include "ZcmObstacleProperties.h"

static int __ZcmObstacleProperties_hash_computed;
static uint64_t __ZcmObstacleProperties_hash;

uint64_t __ZcmObstacleProperties_hash_recursive(const __zcm_hash_ptr* p)
{
    const __zcm_hash_ptr* fp;
    for (fp = p; fp != NULL; fp = fp->parent)
        if (fp->v == __ZcmObstacleProperties_get_hash)
            return 0;

    __zcm_hash_ptr cp;
    cp.parent =  p;
    cp.v = (void*)__ZcmObstacleProperties_get_hash;
    (void) cp;

    uint64_t hash = (uint64_t)0x98f9449d3f585d1aLL
         + __double_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
        ;

    return (hash<<1) + ((hash>>63)&1);
}

int64_t __ZcmObstacleProperties_get_hash(void)
{
    if (!__ZcmObstacleProperties_hash_computed) {
        __ZcmObstacleProperties_hash = (int64_t)__ZcmObstacleProperties_hash_recursive(NULL);
        __ZcmObstacleProperties_hash_computed = 1;
    }

    return __ZcmObstacleProperties_hash;
}

int __ZcmObstacleProperties_encode_array(void* buf, uint32_t offset, uint32_t maxlen, const ZcmObstacleProperties* p, uint32_t elements)
{
    uint32_t pos = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].width), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].length), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].angle), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int ZcmObstacleProperties_encode(void* buf, uint32_t offset, uint32_t maxlen, const ZcmObstacleProperties* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __ZcmObstacleProperties_get_hash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    thislen = __ZcmObstacleProperties_encode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

uint32_t __ZcmObstacleProperties_encoded_array_size(const ZcmObstacleProperties* p, uint32_t elements)
{
    uint32_t size = 0, element;
    for (element = 0; element < elements; ++element) {

        size += __double_encoded_array_size(&(p[element].width), 1);

        size += __double_encoded_array_size(&(p[element].length), 1);

        size += __double_encoded_array_size(&(p[element].angle), 1);

    }
    return size;
}

uint32_t ZcmObstacleProperties_encoded_size(const ZcmObstacleProperties* p)
{
    return 8 + __ZcmObstacleProperties_encoded_array_size(p, 1);
}

uint32_t ZcmObstacleProperties_struct_size(void)
{
    return sizeof(ZcmObstacleProperties);
}

uint32_t ZcmObstacleProperties_num_fields(void)
{
    return 3;
}

int ZcmObstacleProperties_get_field(const ZcmObstacleProperties* p, uint32_t i, zcm_field_t* f)
{
    if (i >= ZcmObstacleProperties_num_fields())
        return 1;
    
    switch (i) {
    
        case 0: {
            f->name = "width";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 0;
            f->data = (void*) &p->width;
            return 0;
        }
        
        case 1: {
            f->name = "length";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 0;
            f->data = (void*) &p->length;
            return 0;
        }
        
        case 2: {
            f->name = "angle";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 0;
            f->data = (void*) &p->angle;
            return 0;
        }
        
        default:
            return 1;
    }
}

const zcm_type_info_t* ZcmObstacleProperties_get_type_info(void)
{
    static int init = 0;
    static zcm_type_info_t typeinfo;
    if (!init) {
        typeinfo.encode         = (zcm_encode_t) ZcmObstacleProperties_encode;
        typeinfo.decode         = (zcm_decode_t) ZcmObstacleProperties_decode;
        typeinfo.decode_cleanup = (zcm_decode_cleanup_t) ZcmObstacleProperties_decode_cleanup;
        typeinfo.encoded_size   = (zcm_encoded_size_t) ZcmObstacleProperties_encoded_size;
        typeinfo.struct_size    = (zcm_struct_size_t)  ZcmObstacleProperties_struct_size;
        typeinfo.num_fields     = (zcm_num_fields_t) ZcmObstacleProperties_num_fields;
        typeinfo.get_field      = (zcm_get_field_t) ZcmObstacleProperties_get_field;
        typeinfo.get_hash       = (zcm_get_hash_t) __ZcmObstacleProperties_get_hash;
    }
    
    return &typeinfo;
}
int __ZcmObstacleProperties_decode_array(const void* buf, uint32_t offset, uint32_t maxlen, ZcmObstacleProperties* p, uint32_t elements)
{
    uint32_t pos = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].width), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].length), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].angle), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int __ZcmObstacleProperties_decode_array_cleanup(ZcmObstacleProperties* p, uint32_t elements)
{
    uint32_t element;
    for (element = 0; element < elements; ++element) {

        __double_decode_array_cleanup(&(p[element].width), 1);

        __double_decode_array_cleanup(&(p[element].length), 1);

        __double_decode_array_cleanup(&(p[element].angle), 1);

    }
    return 0;
}

int ZcmObstacleProperties_decode(const void* buf, uint32_t offset, uint32_t maxlen, ZcmObstacleProperties* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __ZcmObstacleProperties_get_hash();

    int64_t this_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (this_hash != hash) return -1;

    thislen = __ZcmObstacleProperties_decode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int ZcmObstacleProperties_decode_cleanup(ZcmObstacleProperties* p)
{
    return __ZcmObstacleProperties_decode_array_cleanup(p, 1);
}

uint32_t __ZcmObstacleProperties_clone_array(const ZcmObstacleProperties* p, ZcmObstacleProperties* q, uint32_t elements)
{
    uint32_t n = 0, element;
    for (element = 0; element < elements; ++element) {

        n += __double_clone_array(&(p[element].width), &(q[element].width), 1);

        n += __double_clone_array(&(p[element].length), &(q[element].length), 1);

        n += __double_clone_array(&(p[element].angle), &(q[element].angle), 1);

    }
    return n;
}

ZcmObstacleProperties* ZcmObstacleProperties_copy(const ZcmObstacleProperties* p)
{
    ZcmObstacleProperties* q = (ZcmObstacleProperties*) malloc(sizeof(ZcmObstacleProperties));
    __ZcmObstacleProperties_clone_array(p, q, 1);
    return q;
}

void ZcmObstacleProperties_destroy(ZcmObstacleProperties* p)
{
    __ZcmObstacleProperties_decode_array_cleanup(p, 1);
    free(p);
}

int ZcmObstacleProperties_publish(zcm_t* zcm, const char* channel, const ZcmObstacleProperties* p)
{
      uint32_t max_data_size = ZcmObstacleProperties_encoded_size (p);
      uint8_t* buf = (uint8_t*) malloc (max_data_size);
      if (!buf) return -1;
      int data_size = ZcmObstacleProperties_encode (buf, 0, max_data_size, p);
      if (data_size < 0) {
          free (buf);
          return data_size;
      }
      int status = zcm_publish (zcm, channel, buf, (uint32_t)data_size);
      free (buf);
      return status;
}

struct _ZcmObstacleProperties_subscription_t {
    ZcmObstacleProperties_handler_t user_handler;
    void* userdata;
    zcm_sub_t* z_sub;
};
static
void ZcmObstacleProperties_handler_stub (const zcm_recv_buf_t* rbuf,
                            const char* channel, void* userdata)
{
    int status;
    ZcmObstacleProperties p;
    memset(&p, 0, sizeof(ZcmObstacleProperties));
    status = ZcmObstacleProperties_decode (rbuf->data, 0, rbuf->data_size, &p);
    if (status < 0) {
        fprintf (stderr, "error %d decoding ZcmObstacleProperties!!!\n", status);
        return;
    }

    ZcmObstacleProperties_subscription_t* h = (ZcmObstacleProperties_subscription_t*) userdata;
    h->user_handler (rbuf, channel, &p, h->userdata);

    ZcmObstacleProperties_decode_cleanup (&p);
}

ZcmObstacleProperties_subscription_t* ZcmObstacleProperties_subscribe (zcm_t* zcm,
                    const char* channel,
                    ZcmObstacleProperties_handler_t f, void* userdata)
{
    ZcmObstacleProperties_subscription_t* n = (ZcmObstacleProperties_subscription_t*)
                       malloc(sizeof(ZcmObstacleProperties_subscription_t));
    n->user_handler = f;
    n->userdata = userdata;
    n->z_sub = zcm_subscribe (zcm, channel,
                              ZcmObstacleProperties_handler_stub, n);
    if (n->z_sub == NULL) {
        fprintf (stderr,"couldn't reg ZcmObstacleProperties ZCM handler!\n");
        free (n);
        return NULL;
    }
    return n;
}

int ZcmObstacleProperties_unsubscribe(zcm_t* zcm, ZcmObstacleProperties_subscription_t* hid)
{
    int status = zcm_unsubscribe (zcm, hid->z_sub);
    if (0 != status) {
        fprintf(stderr,
           "couldn't unsubscribe ZcmObstacleProperties_handler %p!\n", hid);
        return -1;
    }
    free (hid);
    return 0;
}

