// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by zcm-gen

#include <string.h>
#include "ZcmObstacleUtmCoordinates.h"

static int __ZcmObstacleUtmCoordinates_hash_computed;
static uint64_t __ZcmObstacleUtmCoordinates_hash;

uint64_t __ZcmObstacleUtmCoordinates_hash_recursive(const __zcm_hash_ptr* p)
{
    const __zcm_hash_ptr* fp;
    for (fp = p; fp != NULL; fp = fp->parent)
        if (fp->v == __ZcmObstacleUtmCoordinates_get_hash)
            return 0;

    __zcm_hash_ptr cp;
    cp.parent =  p;
    cp.v = (void*)__ZcmObstacleUtmCoordinates_get_hash;
    (void) cp;

    uint64_t hash = (uint64_t)0x212528dce425de70LL
         + __int32_t_hash_recursive(&cp)
         + __string_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
        ;

    return (hash<<1) + ((hash>>63)&1);
}

int64_t __ZcmObstacleUtmCoordinates_get_hash(void)
{
    if (!__ZcmObstacleUtmCoordinates_hash_computed) {
        __ZcmObstacleUtmCoordinates_hash = (int64_t)__ZcmObstacleUtmCoordinates_hash_recursive(NULL);
        __ZcmObstacleUtmCoordinates_hash_computed = 1;
    }

    return __ZcmObstacleUtmCoordinates_hash;
}

int __ZcmObstacleUtmCoordinates_encode_array(void* buf, uint32_t offset, uint32_t maxlen, const ZcmObstacleUtmCoordinates* p, uint32_t elements)
{
    uint32_t pos = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        thislen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].zone), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __string_encode_array(buf, offset + pos, maxlen - pos, &(p[element].zone_letter), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].easting), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].northing), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int ZcmObstacleUtmCoordinates_encode(void* buf, uint32_t offset, uint32_t maxlen, const ZcmObstacleUtmCoordinates* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __ZcmObstacleUtmCoordinates_get_hash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    thislen = __ZcmObstacleUtmCoordinates_encode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

uint32_t __ZcmObstacleUtmCoordinates_encoded_array_size(const ZcmObstacleUtmCoordinates* p, uint32_t elements)
{
    uint32_t size = 0, element;
    for (element = 0; element < elements; ++element) {

        size += __int32_t_encoded_array_size(&(p[element].zone), 1);

        size += __string_encoded_array_size(&(p[element].zone_letter), 1);

        size += __double_encoded_array_size(&(p[element].easting), 1);

        size += __double_encoded_array_size(&(p[element].northing), 1);

    }
    return size;
}

uint32_t ZcmObstacleUtmCoordinates_encoded_size(const ZcmObstacleUtmCoordinates* p)
{
    return 8 + __ZcmObstacleUtmCoordinates_encoded_array_size(p, 1);
}

uint32_t ZcmObstacleUtmCoordinates_struct_size(void)
{
    return sizeof(ZcmObstacleUtmCoordinates);
}

uint32_t ZcmObstacleUtmCoordinates_num_fields(void)
{
    return 4;
}

int ZcmObstacleUtmCoordinates_get_field(const ZcmObstacleUtmCoordinates* p, uint32_t i, zcm_field_t* f)
{
    if (i >= ZcmObstacleUtmCoordinates_num_fields())
        return 1;
    
    switch (i) {
    
        case 0: {
            f->name = "zone";
            f->type = ZCM_FIELD_INT32_T;
            f->typestr = "int32_t";
            f->num_dim = 0;
            f->data = (void*) &p->zone;
            return 0;
        }
        
        case 1: {
            f->name = "zone_letter";
            f->type = ZCM_FIELD_STRING;
            f->typestr = "string";
            f->num_dim = 0;
            f->data = (void*) &p->zone_letter;
            return 0;
        }
        
        case 2: {
            f->name = "easting";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 0;
            f->data = (void*) &p->easting;
            return 0;
        }
        
        case 3: {
            f->name = "northing";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 0;
            f->data = (void*) &p->northing;
            return 0;
        }
        
        default:
            return 1;
    }
}

const zcm_type_info_t* ZcmObstacleUtmCoordinates_get_type_info(void)
{
    static int init = 0;
    static zcm_type_info_t typeinfo;
    if (!init) {
        typeinfo.encode         = (zcm_encode_t) ZcmObstacleUtmCoordinates_encode;
        typeinfo.decode         = (zcm_decode_t) ZcmObstacleUtmCoordinates_decode;
        typeinfo.decode_cleanup = (zcm_decode_cleanup_t) ZcmObstacleUtmCoordinates_decode_cleanup;
        typeinfo.encoded_size   = (zcm_encoded_size_t) ZcmObstacleUtmCoordinates_encoded_size;
        typeinfo.struct_size    = (zcm_struct_size_t)  ZcmObstacleUtmCoordinates_struct_size;
        typeinfo.num_fields     = (zcm_num_fields_t) ZcmObstacleUtmCoordinates_num_fields;
        typeinfo.get_field      = (zcm_get_field_t) ZcmObstacleUtmCoordinates_get_field;
        typeinfo.get_hash       = (zcm_get_hash_t) __ZcmObstacleUtmCoordinates_get_hash;
    }
    
    return &typeinfo;
}
int __ZcmObstacleUtmCoordinates_decode_array(const void* buf, uint32_t offset, uint32_t maxlen, ZcmObstacleUtmCoordinates* p, uint32_t elements)
{
    uint32_t pos = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        thislen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].zone), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __string_decode_array(buf, offset + pos, maxlen - pos, &(p[element].zone_letter), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].easting), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].northing), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int __ZcmObstacleUtmCoordinates_decode_array_cleanup(ZcmObstacleUtmCoordinates* p, uint32_t elements)
{
    uint32_t element;
    for (element = 0; element < elements; ++element) {

        __int32_t_decode_array_cleanup(&(p[element].zone), 1);

        __string_decode_array_cleanup(&(p[element].zone_letter), 1);

        __double_decode_array_cleanup(&(p[element].easting), 1);

        __double_decode_array_cleanup(&(p[element].northing), 1);

    }
    return 0;
}

int ZcmObstacleUtmCoordinates_decode(const void* buf, uint32_t offset, uint32_t maxlen, ZcmObstacleUtmCoordinates* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __ZcmObstacleUtmCoordinates_get_hash();

    int64_t this_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (this_hash != hash) return -1;

    thislen = __ZcmObstacleUtmCoordinates_decode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int ZcmObstacleUtmCoordinates_decode_cleanup(ZcmObstacleUtmCoordinates* p)
{
    return __ZcmObstacleUtmCoordinates_decode_array_cleanup(p, 1);
}

uint32_t __ZcmObstacleUtmCoordinates_clone_array(const ZcmObstacleUtmCoordinates* p, ZcmObstacleUtmCoordinates* q, uint32_t elements)
{
    uint32_t n = 0, element;
    for (element = 0; element < elements; ++element) {

        n += __int32_t_clone_array(&(p[element].zone), &(q[element].zone), 1);

        n += __string_clone_array(&(p[element].zone_letter), &(q[element].zone_letter), 1);

        n += __double_clone_array(&(p[element].easting), &(q[element].easting), 1);

        n += __double_clone_array(&(p[element].northing), &(q[element].northing), 1);

    }
    return n;
}

ZcmObstacleUtmCoordinates* ZcmObstacleUtmCoordinates_copy(const ZcmObstacleUtmCoordinates* p)
{
    ZcmObstacleUtmCoordinates* q = (ZcmObstacleUtmCoordinates*) malloc(sizeof(ZcmObstacleUtmCoordinates));
    __ZcmObstacleUtmCoordinates_clone_array(p, q, 1);
    return q;
}

void ZcmObstacleUtmCoordinates_destroy(ZcmObstacleUtmCoordinates* p)
{
    __ZcmObstacleUtmCoordinates_decode_array_cleanup(p, 1);
    free(p);
}

int ZcmObstacleUtmCoordinates_publish(zcm_t* zcm, const char* channel, const ZcmObstacleUtmCoordinates* p)
{
      uint32_t max_data_size = ZcmObstacleUtmCoordinates_encoded_size (p);
      uint8_t* buf = (uint8_t*) malloc (max_data_size);
      if (!buf) return -1;
      int data_size = ZcmObstacleUtmCoordinates_encode (buf, 0, max_data_size, p);
      if (data_size < 0) {
          free (buf);
          return data_size;
      }
      int status = zcm_publish (zcm, channel, buf, (uint32_t)data_size);
      free (buf);
      return status;
}

struct _ZcmObstacleUtmCoordinates_subscription_t {
    ZcmObstacleUtmCoordinates_handler_t user_handler;
    void* userdata;
    zcm_sub_t* z_sub;
};
static
void ZcmObstacleUtmCoordinates_handler_stub (const zcm_recv_buf_t* rbuf,
                            const char* channel, void* userdata)
{
    int status;
    ZcmObstacleUtmCoordinates p;
    memset(&p, 0, sizeof(ZcmObstacleUtmCoordinates));
    status = ZcmObstacleUtmCoordinates_decode (rbuf->data, 0, rbuf->data_size, &p);
    if (status < 0) {
        fprintf (stderr, "error %d decoding ZcmObstacleUtmCoordinates!!!\n", status);
        return;
    }

    ZcmObstacleUtmCoordinates_subscription_t* h = (ZcmObstacleUtmCoordinates_subscription_t*) userdata;
    h->user_handler (rbuf, channel, &p, h->userdata);

    ZcmObstacleUtmCoordinates_decode_cleanup (&p);
}

ZcmObstacleUtmCoordinates_subscription_t* ZcmObstacleUtmCoordinates_subscribe (zcm_t* zcm,
                    const char* channel,
                    ZcmObstacleUtmCoordinates_handler_t f, void* userdata)
{
    ZcmObstacleUtmCoordinates_subscription_t* n = (ZcmObstacleUtmCoordinates_subscription_t*)
                       malloc(sizeof(ZcmObstacleUtmCoordinates_subscription_t));
    n->user_handler = f;
    n->userdata = userdata;
    n->z_sub = zcm_subscribe (zcm, channel,
                              ZcmObstacleUtmCoordinates_handler_stub, n);
    if (n->z_sub == NULL) {
        fprintf (stderr,"couldn't reg ZcmObstacleUtmCoordinates ZCM handler!\n");
        free (n);
        return NULL;
    }
    return n;
}

int ZcmObstacleUtmCoordinates_unsubscribe(zcm_t* zcm, ZcmObstacleUtmCoordinates_subscription_t* hid)
{
    int status = zcm_unsubscribe (zcm, hid->z_sub);
    if (0 != status) {
        fprintf(stderr,
           "couldn't unsubscribe ZcmObstacleUtmCoordinates_handler %p!\n", hid);
        return -1;
    }
    free (hid);
    return 0;
}

