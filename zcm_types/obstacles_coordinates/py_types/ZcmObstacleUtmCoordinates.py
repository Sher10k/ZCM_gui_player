"""ZCM type definitions
This file automatically generated by zcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class ZcmObstacleUtmCoordinates(object):
    __slots__ = ["zone", "zone_letter", "easting", "northing"]

    def __init__(self):
        self.zone = 0
        self.zone_letter = ""
        self.easting = 0.0
        self.northing = 0.0

    def encode(self):
        buf = BytesIO()
        buf.write(ZcmObstacleUtmCoordinates._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">i", self.zone))
        __zone_letter_encoded = self.zone_letter.encode('utf-8')
        buf.write(struct.pack('>I', len(__zone_letter_encoded)+1))
        buf.write(__zone_letter_encoded)
        buf.write(b"\0")
        buf.write(struct.pack(">dd", self.easting, self.northing))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != ZcmObstacleUtmCoordinates._get_packed_fingerprint():
            raise ValueError("Decode error")
        return ZcmObstacleUtmCoordinates._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = ZcmObstacleUtmCoordinates()
        self.zone = struct.unpack(">i", buf.read(4))[0]
        __zone_letter_len = struct.unpack('>I', buf.read(4))[0]
        self.zone_letter = buf.read(__zone_letter_len)[:-1].decode('utf-8', 'replace')
        self.easting, self.northing = struct.unpack(">dd", buf.read(16))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if ZcmObstacleUtmCoordinates in parents: return 0
        tmphash = (0x212528dce425de70) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + ((tmphash>>63)&0x1)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if ZcmObstacleUtmCoordinates._packed_fingerprint is None:
            ZcmObstacleUtmCoordinates._packed_fingerprint = struct.pack(">Q", ZcmObstacleUtmCoordinates._get_hash_recursive([]))
        return ZcmObstacleUtmCoordinates._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

