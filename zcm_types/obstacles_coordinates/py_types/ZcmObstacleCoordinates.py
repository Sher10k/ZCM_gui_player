"""ZCM type definitions
This file automatically generated by zcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

from ZcmObstacle import ZcmObstacle

from ZcmService import ZcmService

class ZcmObstacleCoordinates(object):
    __slots__ = ["service", "objects_count", "objects"]

    def __init__(self):
        self.service = ZcmService()
        self.objects_count = 0
        self.objects = []

    def encode(self):
        buf = BytesIO()
        buf.write(ZcmObstacleCoordinates._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        assert self.service._get_packed_fingerprint() == ZcmService._get_packed_fingerprint()
        self.service._encode_one(buf)
        buf.write(struct.pack(">i", self.objects_count))
        for i0 in range(self.objects_count):
            assert self.objects[i0]._get_packed_fingerprint() == ZcmObstacle._get_packed_fingerprint()
            self.objects[i0]._encode_one(buf)

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != ZcmObstacleCoordinates._get_packed_fingerprint():
            raise ValueError("Decode error")
        return ZcmObstacleCoordinates._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = ZcmObstacleCoordinates()
        self.service = ZcmService._decode_one(buf)
        self.objects_count = struct.unpack(">i", buf.read(4))[0]
        self.objects = []
        for i0 in range(self.objects_count):
            self.objects.append(ZcmObstacle._decode_one(buf))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if ZcmObstacleCoordinates in parents: return 0
        newparents = parents + [ZcmObstacleCoordinates]
        tmphash = (0xb22b80aa673e1873+ ZcmService._get_hash_recursive(newparents)+ ZcmObstacle._get_hash_recursive(newparents)) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + ((tmphash>>63)&0x1)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if ZcmObstacleCoordinates._packed_fingerprint is None:
            ZcmObstacleCoordinates._packed_fingerprint = struct.pack(">Q", ZcmObstacleCoordinates._get_hash_recursive([]))
        return ZcmObstacleCoordinates._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

