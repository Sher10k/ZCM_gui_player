"""ZCM type definitions
This file automatically generated by zcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class ZcmLidarVuPoint(object):
    __slots__ = ["channel", "x", "y", "z", "amplitude", "flag_info"]

    def __init__(self):
        self.channel = 0
        self.x = 0.0
        self.y = 0.0
        self.z = 0.0
        self.amplitude = 0.0
        self.flag_info = 0

    def encode(self):
        buf = BytesIO()
        buf.write(ZcmLidarVuPoint._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">bffffb", self.channel, self.x, self.y, self.z, self.amplitude, self.flag_info))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != ZcmLidarVuPoint._get_packed_fingerprint():
            raise ValueError("Decode error")
        return ZcmLidarVuPoint._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = ZcmLidarVuPoint()
        self.channel, self.x, self.y, self.z, self.amplitude, self.flag_info = struct.unpack(">bffffb", buf.read(18))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if ZcmLidarVuPoint in parents: return 0
        tmphash = (0xd58291ae7847d379) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + ((tmphash>>63)&0x1)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if ZcmLidarVuPoint._packed_fingerprint is None:
            ZcmLidarVuPoint._packed_fingerprint = struct.pack(">Q", ZcmLidarVuPoint._get_hash_recursive([]))
        return ZcmLidarVuPoint._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

