// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by zcm-gen

#include <string.h>
#include "ZcmImuNXP.h"

static int __ZcmImuNXP_hash_computed;
static uint64_t __ZcmImuNXP_hash;

uint64_t __ZcmImuNXP_hash_recursive(const __zcm_hash_ptr* p)
{
    const __zcm_hash_ptr* fp;
    for (fp = p; fp != NULL; fp = fp->parent)
        if (fp->v == __ZcmImuNXP_get_hash)
            return 0;

    __zcm_hash_ptr cp;
    cp.parent =  p;
    cp.v = (void*)__ZcmImuNXP_get_hash;
    (void) cp;

    uint64_t hash = (uint64_t)0xeebf38eaa4346fd6LL
         + __ZcmService_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
         + __boolean_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
        ;

    return (hash<<1) + ((hash>>63)&1);
}

int64_t __ZcmImuNXP_get_hash(void)
{
    if (!__ZcmImuNXP_hash_computed) {
        __ZcmImuNXP_hash = (int64_t)__ZcmImuNXP_hash_recursive(NULL);
        __ZcmImuNXP_hash_computed = 1;
    }

    return __ZcmImuNXP_hash;
}

int __ZcmImuNXP_encode_array(void* buf, uint32_t offset, uint32_t maxlen, const ZcmImuNXP* p, uint32_t elements)
{
    uint32_t pos = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        thislen = __ZcmService_encode_array(buf, offset + pos, maxlen - pos, &(p[element].service), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].acc_x), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].acc_y), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].acc_z), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].magn_x), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].magn_y), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].magn_z), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &(p[element].has_gyro), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].gyro_x), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].gyro_y), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].gyro_z), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int ZcmImuNXP_encode(void* buf, uint32_t offset, uint32_t maxlen, const ZcmImuNXP* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __ZcmImuNXP_get_hash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    thislen = __ZcmImuNXP_encode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

uint32_t __ZcmImuNXP_encoded_array_size(const ZcmImuNXP* p, uint32_t elements)
{
    uint32_t size = 0, element;
    for (element = 0; element < elements; ++element) {

        size += __ZcmService_encoded_array_size(&(p[element].service), 1);

        size += __double_encoded_array_size(&(p[element].acc_x), 1);

        size += __double_encoded_array_size(&(p[element].acc_y), 1);

        size += __double_encoded_array_size(&(p[element].acc_z), 1);

        size += __double_encoded_array_size(&(p[element].magn_x), 1);

        size += __double_encoded_array_size(&(p[element].magn_y), 1);

        size += __double_encoded_array_size(&(p[element].magn_z), 1);

        size += __boolean_encoded_array_size(&(p[element].has_gyro), 1);

        size += __double_encoded_array_size(&(p[element].gyro_x), 1);

        size += __double_encoded_array_size(&(p[element].gyro_y), 1);

        size += __double_encoded_array_size(&(p[element].gyro_z), 1);

    }
    return size;
}

uint32_t ZcmImuNXP_encoded_size(const ZcmImuNXP* p)
{
    return 8 + __ZcmImuNXP_encoded_array_size(p, 1);
}

uint32_t ZcmImuNXP_struct_size(void)
{
    return sizeof(ZcmImuNXP);
}

uint32_t ZcmImuNXP_num_fields(void)
{
    return 11;
}

int ZcmImuNXP_get_field(const ZcmImuNXP* p, uint32_t i, zcm_field_t* f)
{
    if (i >= ZcmImuNXP_num_fields())
        return 1;
    
    switch (i) {
    
        case 0: {
            /* ZcmService */
            f->name = "service";
            f->type = ZCM_FIELD_USER_TYPE;
            f->typestr = "ZcmService";
            f->num_dim = 0;
            f->data = (void*) &p->service;
            return 0;
        }
        
        case 1: {
            f->name = "acc_x";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 0;
            f->data = (void*) &p->acc_x;
            return 0;
        }
        
        case 2: {
            f->name = "acc_y";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 0;
            f->data = (void*) &p->acc_y;
            return 0;
        }
        
        case 3: {
            f->name = "acc_z";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 0;
            f->data = (void*) &p->acc_z;
            return 0;
        }
        
        case 4: {
            f->name = "magn_x";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 0;
            f->data = (void*) &p->magn_x;
            return 0;
        }
        
        case 5: {
            f->name = "magn_y";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 0;
            f->data = (void*) &p->magn_y;
            return 0;
        }
        
        case 6: {
            f->name = "magn_z";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 0;
            f->data = (void*) &p->magn_z;
            return 0;
        }
        
        case 7: {
            f->name = "has_gyro";
            f->type = ZCM_FIELD_BOOLEAN;
            f->typestr = "boolean";
            f->num_dim = 0;
            f->data = (void*) &p->has_gyro;
            return 0;
        }
        
        case 8: {
            f->name = "gyro_x";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 0;
            f->data = (void*) &p->gyro_x;
            return 0;
        }
        
        case 9: {
            f->name = "gyro_y";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 0;
            f->data = (void*) &p->gyro_y;
            return 0;
        }
        
        case 10: {
            f->name = "gyro_z";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 0;
            f->data = (void*) &p->gyro_z;
            return 0;
        }
        
        default:
            return 1;
    }
}

const zcm_type_info_t* ZcmImuNXP_get_type_info(void)
{
    static int init = 0;
    static zcm_type_info_t typeinfo;
    if (!init) {
        typeinfo.encode         = (zcm_encode_t) ZcmImuNXP_encode;
        typeinfo.decode         = (zcm_decode_t) ZcmImuNXP_decode;
        typeinfo.decode_cleanup = (zcm_decode_cleanup_t) ZcmImuNXP_decode_cleanup;
        typeinfo.encoded_size   = (zcm_encoded_size_t) ZcmImuNXP_encoded_size;
        typeinfo.struct_size    = (zcm_struct_size_t)  ZcmImuNXP_struct_size;
        typeinfo.num_fields     = (zcm_num_fields_t) ZcmImuNXP_num_fields;
        typeinfo.get_field      = (zcm_get_field_t) ZcmImuNXP_get_field;
        typeinfo.get_hash       = (zcm_get_hash_t) __ZcmImuNXP_get_hash;
    }
    
    return &typeinfo;
}
int __ZcmImuNXP_decode_array(const void* buf, uint32_t offset, uint32_t maxlen, ZcmImuNXP* p, uint32_t elements)
{
    uint32_t pos = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        thislen = __ZcmService_decode_array(buf, offset + pos, maxlen - pos, &(p[element].service), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].acc_x), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].acc_y), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].acc_z), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].magn_x), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].magn_y), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].magn_z), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &(p[element].has_gyro), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].gyro_x), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].gyro_y), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].gyro_z), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int __ZcmImuNXP_decode_array_cleanup(ZcmImuNXP* p, uint32_t elements)
{
    uint32_t element;
    for (element = 0; element < elements; ++element) {

        __ZcmService_decode_array_cleanup(&(p[element].service), 1);

        __double_decode_array_cleanup(&(p[element].acc_x), 1);

        __double_decode_array_cleanup(&(p[element].acc_y), 1);

        __double_decode_array_cleanup(&(p[element].acc_z), 1);

        __double_decode_array_cleanup(&(p[element].magn_x), 1);

        __double_decode_array_cleanup(&(p[element].magn_y), 1);

        __double_decode_array_cleanup(&(p[element].magn_z), 1);

        __boolean_decode_array_cleanup(&(p[element].has_gyro), 1);

        __double_decode_array_cleanup(&(p[element].gyro_x), 1);

        __double_decode_array_cleanup(&(p[element].gyro_y), 1);

        __double_decode_array_cleanup(&(p[element].gyro_z), 1);

    }
    return 0;
}

int ZcmImuNXP_decode(const void* buf, uint32_t offset, uint32_t maxlen, ZcmImuNXP* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __ZcmImuNXP_get_hash();

    int64_t this_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (this_hash != hash) return -1;

    thislen = __ZcmImuNXP_decode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int ZcmImuNXP_decode_cleanup(ZcmImuNXP* p)
{
    return __ZcmImuNXP_decode_array_cleanup(p, 1);
}

uint32_t __ZcmImuNXP_clone_array(const ZcmImuNXP* p, ZcmImuNXP* q, uint32_t elements)
{
    uint32_t n = 0, element;
    for (element = 0; element < elements; ++element) {

        n += __ZcmService_clone_array(&(p[element].service), &(q[element].service), 1);

        n += __double_clone_array(&(p[element].acc_x), &(q[element].acc_x), 1);

        n += __double_clone_array(&(p[element].acc_y), &(q[element].acc_y), 1);

        n += __double_clone_array(&(p[element].acc_z), &(q[element].acc_z), 1);

        n += __double_clone_array(&(p[element].magn_x), &(q[element].magn_x), 1);

        n += __double_clone_array(&(p[element].magn_y), &(q[element].magn_y), 1);

        n += __double_clone_array(&(p[element].magn_z), &(q[element].magn_z), 1);

        n += __boolean_clone_array(&(p[element].has_gyro), &(q[element].has_gyro), 1);

        n += __double_clone_array(&(p[element].gyro_x), &(q[element].gyro_x), 1);

        n += __double_clone_array(&(p[element].gyro_y), &(q[element].gyro_y), 1);

        n += __double_clone_array(&(p[element].gyro_z), &(q[element].gyro_z), 1);

    }
    return n;
}

ZcmImuNXP* ZcmImuNXP_copy(const ZcmImuNXP* p)
{
    ZcmImuNXP* q = (ZcmImuNXP*) malloc(sizeof(ZcmImuNXP));
    __ZcmImuNXP_clone_array(p, q, 1);
    return q;
}

void ZcmImuNXP_destroy(ZcmImuNXP* p)
{
    __ZcmImuNXP_decode_array_cleanup(p, 1);
    free(p);
}

int ZcmImuNXP_publish(zcm_t* zcm, const char* channel, const ZcmImuNXP* p)
{
      uint32_t max_data_size = ZcmImuNXP_encoded_size (p);
      uint8_t* buf = (uint8_t*) malloc (max_data_size);
      if (!buf) return -1;
      int data_size = ZcmImuNXP_encode (buf, 0, max_data_size, p);
      if (data_size < 0) {
          free (buf);
          return data_size;
      }
      int status = zcm_publish (zcm, channel, buf, (uint32_t)data_size);
      free (buf);
      return status;
}

struct _ZcmImuNXP_subscription_t {
    ZcmImuNXP_handler_t user_handler;
    void* userdata;
    zcm_sub_t* z_sub;
};
static
void ZcmImuNXP_handler_stub (const zcm_recv_buf_t* rbuf,
                            const char* channel, void* userdata)
{
    int status;
    ZcmImuNXP p;
    memset(&p, 0, sizeof(ZcmImuNXP));
    status = ZcmImuNXP_decode (rbuf->data, 0, rbuf->data_size, &p);
    if (status < 0) {
        fprintf (stderr, "error %d decoding ZcmImuNXP!!!\n", status);
        return;
    }

    ZcmImuNXP_subscription_t* h = (ZcmImuNXP_subscription_t*) userdata;
    h->user_handler (rbuf, channel, &p, h->userdata);

    ZcmImuNXP_decode_cleanup (&p);
}

ZcmImuNXP_subscription_t* ZcmImuNXP_subscribe (zcm_t* zcm,
                    const char* channel,
                    ZcmImuNXP_handler_t f, void* userdata)
{
    ZcmImuNXP_subscription_t* n = (ZcmImuNXP_subscription_t*)
                       malloc(sizeof(ZcmImuNXP_subscription_t));
    n->user_handler = f;
    n->userdata = userdata;
    n->z_sub = zcm_subscribe (zcm, channel,
                              ZcmImuNXP_handler_stub, n);
    if (n->z_sub == NULL) {
        fprintf (stderr,"couldn't reg ZcmImuNXP ZCM handler!\n");
        free (n);
        return NULL;
    }
    return n;
}

int ZcmImuNXP_unsubscribe(zcm_t* zcm, ZcmImuNXP_subscription_t* hid)
{
    int status = zcm_unsubscribe (zcm, hid->z_sub);
    if (0 != status) {
        fprintf(stderr,
           "couldn't unsubscribe ZcmImuNXP_handler %p!\n", hid);
        return -1;
    }
    free (hid);
    return 0;
}

